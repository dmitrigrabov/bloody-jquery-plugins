---
type: operation
title: "List Sessions"
operationId: "list_sessions_apps__app_name__users__user_id__sessions_get"
method: GET
path: "/apps/{app_name}/users/{user_id}/sessions"
---

# List Sessions

`GET` `/apps/{app_name}/users/{user_id}/sessions`

## Path parameters

**app_name** `string` required _App Name_

**user_id** `string` required _User Id_

## Responses

### `200` — Successful Response

[`Session[]`](#session) _Response List Sessions Apps  App Name  Users  User Id  Sessions Get_

### `422` — Validation Error

[`HTTPValidationError`](#httpvalidationerror) _HTTPValidationError_

## Referenced types

### Session

`object` _Session_ — Represents a series of interactions between a user and agents.
- **id** `string` required _Id_ — Unique identifier of the session.
- **appName** `string` required _Appname_ — Application name that owns the session.
- **userId** `string` required _Userid_ — User ID that owns the session.
- **state** `object` _State_ — Current persisted session state.
- **events** [`EventOutput[]`](#eventoutput) _Events_ — Ordered event history for the session, including user, model, and tool events (e.g. user input, model response, function call/response).
- **lastUpdateTime** `number` (default: `0`) _Lastupdatetime_ — Unix timestamp in seconds for the most recent session update.

### HTTPValidationError

`object` _HTTPValidationError_
- **detail** [`ValidationError[]`](#validationerror) _Detail_

### EventOutput

`object` _Event_ — Represents an event in a conversation between agents and users.

It is used to store the content of the conversation, as well as the actions
taken by the agents like function calls, etc.
- **modelVersion** `string` nullable _Modelversion_
- **content** [`Content`](#content) _Content_ — Contains the multi-part content of a message.
- **groundingMetadata** [`GroundingMetadata`](#groundingmetadata) _GroundingMetadata_ — Information for various kinds of grounding.
- **partial** `boolean` nullable _Partial_
- **turnComplete** `boolean` nullable _Turncomplete_
- **turnCompleteReason** [`TurnCompleteReason`](#turncompletereason) (`TURN_COMPLETE_REASON_UNSPECIFIED`, `MALFORMED_FUNCTION_CALL`, `RESPONSE_REJECTED`, `NEED_MORE_INPUT`, `PROHIBITED_INPUT_CONTENT`, `IMAGE_PROHIBITED_INPUT_CONTENT`, `INPUT_TEXT_CONTAIN_PROMINENT_PERSON_PROHIBITED`, `INPUT_IMAGE_CELEBRITY`, `INPUT_IMAGE_PHOTO_REALISTIC_CHILD_PROHIBITED`, `INPUT_TEXT_NCII_PROHIBITED`, `INPUT_OTHER`, `INPUT_IP_PROHIBITED`, `BLOCKLIST`, `UNSAFE_PROMPT_FOR_IMAGE_GENERATION`, `GENERATED_IMAGE_SAFETY`, `GENERATED_CONTENT_SAFETY`, `GENERATED_AUDIO_SAFETY`, `GENERATED_VIDEO_SAFETY`, `GENERATED_CONTENT_PROHIBITED`, `GENERATED_CONTENT_BLOCKLIST`, `GENERATED_IMAGE_PROHIBITED`, `GENERATED_IMAGE_CELEBRITY`, `GENERATED_IMAGE_PROMINENT_PEOPLE_DETECTED_BY_REWRITER`, `GENERATED_IMAGE_IDENTIFIABLE_PEOPLE`, `GENERATED_IMAGE_MINORS`, `OUTPUT_IMAGE_IP_PROHIBITED`, `GENERATED_OTHER`, `MAX_REGENERATION_REACHED`) _TurnCompleteReason_ — The reason why the turn is complete.
- **finishReason** [`FinishReason`](#finishreason) (`FINISH_REASON_UNSPECIFIED`, `STOP`, `MAX_TOKENS`, `SAFETY`, `RECITATION`, `LANGUAGE`, `OTHER`, `BLOCKLIST`, `PROHIBITED_CONTENT`, `SPII`, `MALFORMED_FUNCTION_CALL`, `IMAGE_SAFETY`, `UNEXPECTED_TOOL_CALL`, `IMAGE_PROHIBITED_CONTENT`, `NO_IMAGE`, `IMAGE_RECITATION`, `IMAGE_OTHER`) _FinishReason_ — Output only. The reason why the model stopped generating tokens.

If empty, the model has not stopped generating the tokens.
- **errorCode** `string` nullable _Errorcode_
- **errorMessage** `string` nullable _Errormessage_
- **interrupted** `boolean` nullable _Interrupted_
- **customMetadata** `object` nullable _Custommetadata_
- **usageMetadata** [`GenerateContentResponseUsageMetadata`](#generatecontentresponseusagemetadata) _GenerateContentResponseUsageMetadata_ — Usage metadata about the content generation request and response.

This message provides a detailed breakdown of token usage and other relevant
metrics. This data type is not supported in Gemini API.
- **liveSessionResumptionUpdate** [`LiveServerSessionResumptionUpdate`](#liveserversessionresumptionupdate) _LiveServerSessionResumptionUpdate_ — Update of the session resumption state.

Only sent if `session_resumption` was set in the connection config.
- **liveSessionId** `string` nullable _Livesessionid_
- **goAway** [`LiveServerGoAway`](#liveservergoaway) _LiveServerGoAway_ — Server will not be able to service client soon.
- **inputTranscription** [`Transcription`](#transcription) _Transcription_ — Audio transcription in Server Content.
- **outputTranscription** [`Transcription`](#transcription) _Transcription_ — Audio transcription in Server Content.
- **avgLogprobs** `number` nullable _Avglogprobs_
- **logprobsResult** [`LogprobsResult`](#logprobsresult) _LogprobsResult_ — The log probabilities of the tokens generated by the model.

This is useful for understanding the model's confidence in its predictions and
for debugging. For example, you can use log probabilities to identify when the
model is making a less confident prediction or to explore alternative
responses that the model considered. A low log probability can also indicate
that the model is "hallucinating" or generating factually incorrect
information.
- **cacheMetadata** [`CacheMetadata`](#cachemetadata) _CacheMetadata_ — Metadata for context cache associated with LLM responses.

This class stores cache identification, usage tracking, and lifecycle
information for a particular cache instance. It can be in two states:

1. Active cache state: cache_name is set, all fields populated
2. Fingerprint-only state: cache_name is None, only fingerprint and
   contents_count are set for prefix matching

Token counts (cached and total) are available in the LlmResponse.usage_metadata
and should be accessed from there to avoid duplication.

Attributes:
    cache_name: The full resource name of the cached content (e.g.,
        'projects/123/locations/us-central1/cachedContents/456').
        None when no active cache exists (fingerprint-only state).
    expire_time: Unix timestamp when the cache expires. None when no
        active cache exists.
    fingerprint: Hash of cacheable contents (instruction + tools + contents).
        Always present for prefix matching.
    invocations_used: Number of invocations this cache has been used for.
        None when no active cache exists.
    contents_count: Number of contents. When active cache exists, this is
        the count of cached contents. When no active cache exists, this is
        the total count of contents in the request.
    created_at: Unix timestamp when the cache was created. None when
        no active cache exists.
- **citationMetadata** [`CitationMetadata`](#citationmetadata) _CitationMetadata_ — Citation information when the model quotes another source.
- **interactionId** `string` nullable _Interactionid_
- **invocationId** `string` (default: ``) _Invocationid_
- **author** `string` (default: ``) _Author_
- **actions** [`EventActionsOutput`](#eventactionsoutput) _EventActions_ — Represents the actions attached to an event.
- **output** `unknown` nullable _Output_
- **nodeInfo** [`NodeInfo`](#nodeinfo) _NodeInfo_ — Workflow node metadata attached to an Event.
- **longRunningToolIds** `string[]` nullable (unique items) _Longrunningtoolids_
- **branch** `string` nullable _Branch_
- **isolationScope** `string` nullable _Isolationscope_
- **id** `string` (default: ``) _Id_
- **timestamp** `number` _Timestamp_

### ValidationError

`object` _ValidationError_
- **loc** `union[]` required _Location_
  - `string`
  - `integer`
- **msg** `string` required _Message_
- **type** `string` required _Error Type_
- **input** `unknown` _Input_
- **ctx** `object` _Context_

### Content

`object` _Content_ — Contains the multi-part content of a message.
- **parts** [`Part[]`](#part) nullable _Parts_ — List of parts that constitute a single message. Each part may have
      a different IANA MIME type.
- **role** `string` nullable _Role_ — Optional. The producer of the content. Must be either 'user' or 'model'. If not set, the service will default to 'user'.

### GroundingMetadata

`object` _GroundingMetadata_ — Information for various kinds of grounding.
- **imageSearchQueries** `string[]` nullable _Imagesearchqueries_ — Optional. The image search queries that were used to generate the content. This field is populated only when the grounding source is Google Search with the Image Search search_type enabled.
- **groundingChunks** [`GroundingChunk[]`](#groundingchunk) nullable _Groundingchunks_ — A list of supporting references retrieved from the grounding
      source. This field is populated when the grounding source is Google
      Search, Vertex AI Search, or Google Maps.
- **groundingSupports** [`GroundingSupport[]`](#groundingsupport) nullable _Groundingsupports_ — List of grounding support.
- **retrievalMetadata** [`RetrievalMetadata`](#retrievalmetadata) _RetrievalMetadata_ — Metadata returned to client when grounding is enabled.
- **searchEntryPoint** [`SearchEntryPoint`](#searchentrypoint) _SearchEntryPoint_ — The entry point used to search for grounding sources.
- **webSearchQueries** `string[]` nullable _Websearchqueries_ — Web search queries for the following-up web search.
- **googleMapsWidgetContextToken** `string` nullable _Googlemapswidgetcontexttoken_ — Optional. Output only. A token that can be used to render a Google Maps widget with the contextual data. This field is populated only when the grounding source is Google Maps.
- **retrievalQueries** `string[]` nullable _Retrievalqueries_ — Optional. The queries that were executed by the retrieval tools. This field is populated only when the grounding source is a retrieval tool, such as Vertex AI Search. This field is not supported in Gemini API.
- **sourceFlaggingUris** [`GroundingMetadataSourceFlaggingUri[]`](#groundingmetadatasourceflagginguri) nullable _Sourceflagginguris_ — Optional. Output only. A list of URIs that can be used to flag a place or review for inappropriate content. This field is populated only when the grounding source is Google Maps. This field is not supported in Gemini API.

### TurnCompleteReason

`string` (`TURN_COMPLETE_REASON_UNSPECIFIED`, `MALFORMED_FUNCTION_CALL`, `RESPONSE_REJECTED`, `NEED_MORE_INPUT`, `PROHIBITED_INPUT_CONTENT`, `IMAGE_PROHIBITED_INPUT_CONTENT`, `INPUT_TEXT_CONTAIN_PROMINENT_PERSON_PROHIBITED`, `INPUT_IMAGE_CELEBRITY`, `INPUT_IMAGE_PHOTO_REALISTIC_CHILD_PROHIBITED`, `INPUT_TEXT_NCII_PROHIBITED`, `INPUT_OTHER`, `INPUT_IP_PROHIBITED`, `BLOCKLIST`, `UNSAFE_PROMPT_FOR_IMAGE_GENERATION`, `GENERATED_IMAGE_SAFETY`, `GENERATED_CONTENT_SAFETY`, `GENERATED_AUDIO_SAFETY`, `GENERATED_VIDEO_SAFETY`, `GENERATED_CONTENT_PROHIBITED`, `GENERATED_CONTENT_BLOCKLIST`, `GENERATED_IMAGE_PROHIBITED`, `GENERATED_IMAGE_CELEBRITY`, `GENERATED_IMAGE_PROMINENT_PEOPLE_DETECTED_BY_REWRITER`, `GENERATED_IMAGE_IDENTIFIABLE_PEOPLE`, `GENERATED_IMAGE_MINORS`, `OUTPUT_IMAGE_IP_PROHIBITED`, `GENERATED_OTHER`, `MAX_REGENERATION_REACHED`) _TurnCompleteReason_ — The reason why the turn is complete.

### FinishReason

`string` (`FINISH_REASON_UNSPECIFIED`, `STOP`, `MAX_TOKENS`, `SAFETY`, `RECITATION`, `LANGUAGE`, `OTHER`, `BLOCKLIST`, `PROHIBITED_CONTENT`, `SPII`, `MALFORMED_FUNCTION_CALL`, `IMAGE_SAFETY`, `UNEXPECTED_TOOL_CALL`, `IMAGE_PROHIBITED_CONTENT`, `NO_IMAGE`, `IMAGE_RECITATION`, `IMAGE_OTHER`) _FinishReason_ — Output only. The reason why the model stopped generating tokens.

If empty, the model has not stopped generating the tokens.

### GenerateContentResponseUsageMetadata

`object` _GenerateContentResponseUsageMetadata_ — Usage metadata about the content generation request and response.

This message provides a detailed breakdown of token usage and other relevant
metrics. This data type is not supported in Gemini API.
- **cacheTokensDetails** [`ModalityTokenCount[]`](#modalitytokencount) nullable _Cachetokensdetails_ — Output only. A detailed breakdown of the token count for each modality in the cached content.
- **cachedContentTokenCount** `integer` nullable _Cachedcontenttokencount_ — Output only. The number of tokens in the cached content that was used for this request.
- **candidatesTokenCount** `integer` nullable _Candidatestokencount_ — The total number of tokens in the generated candidates.
- **candidatesTokensDetails** [`ModalityTokenCount[]`](#modalitytokencount) nullable _Candidatestokensdetails_ — Output only. A detailed breakdown of the token count for each modality in the generated candidates.
- **promptTokenCount** `integer` nullable _Prompttokencount_ — The total number of tokens in the prompt. This includes any text, images, or other media provided in the request. When `cached_content` is set, this also includes the number of tokens in the cached content.
- **promptTokensDetails** [`ModalityTokenCount[]`](#modalitytokencount) nullable _Prompttokensdetails_ — Output only. A detailed breakdown of the token count for each modality in the prompt.
- **thoughtsTokenCount** `integer` nullable _Thoughtstokencount_ — Output only. The number of tokens that were part of the model's generated "thoughts" output, if applicable.
- **toolUsePromptTokenCount** `integer` nullable _Tooluseprompttokencount_ — Output only. The number of tokens in the results from tool executions, which are provided back to the model as input, if applicable.
- **toolUsePromptTokensDetails** [`ModalityTokenCount[]`](#modalitytokencount) nullable _Tooluseprompttokensdetails_ — Output only. A detailed breakdown by modality of the token counts from the results of tool executions, which are provided back to the model as input.
- **totalTokenCount** `integer` nullable _Totaltokencount_ — The total number of tokens for the entire request. This is the sum of `prompt_token_count`, `candidates_token_count`, `tool_use_prompt_token_count`, and `thoughts_token_count`.
- **trafficType** [`TrafficType`](#traffictype) (`TRAFFIC_TYPE_UNSPECIFIED`, `ON_DEMAND`, `ON_DEMAND_PRIORITY`, `ON_DEMAND_FLEX`, `PROVISIONED_THROUGHPUT`) _TrafficType_ — Output only.

The traffic type for this request. This enum is not supported in Gemini API.

### LiveServerSessionResumptionUpdate

`object` _LiveServerSessionResumptionUpdate_ — Update of the session resumption state.

Only sent if `session_resumption` was set in the connection config.
- **newHandle** `string` nullable _Newhandle_ — New handle that represents state that can be resumed. Empty if `resumable`=false.
- **resumable** `boolean` nullable _Resumable_ — True if session can be resumed at this point. It might be not possible to resume session at some points. In that case we send update empty new_handle and resumable=false. Example of such case could be model executing function calls or just generating. Resuming session (using previous session token) in such state will result in some data loss.
- **lastConsumedClientMessageIndex** `integer` nullable _Lastconsumedclientmessageindex_ — Index of last message sent by client that is included in state represented by this SessionResumptionToken. Only sent when `SessionResumptionConfig.transparent` is set.

Presence of this index allows users to transparently reconnect and avoid issue of losing some part of realtime audio input/video. If client wishes to temporarily disconnect (for example as result of receiving GoAway) they can do it without losing state by buffering messages sent since last `SessionResumptionTokenUpdate`. This field will enable them to limit buffering (avoid keeping all requests in RAM).

Note: This should not be used for when resuming a session at some time later -- in those cases partial audio and video frames are likely not needed.

### LiveServerGoAway

`object` _LiveServerGoAway_ — Server will not be able to service client soon.
- **timeLeft** `string` nullable _Timeleft_ — The remaining time before the connection will be terminated as ABORTED. The minimal time returned here is specified differently together with the rate limits for a given model.

### Transcription

`object` _Transcription_ — Audio transcription in Server Content.
- **text** `string` nullable _Text_ — Optional. Transcription text.
- **finished** `boolean` nullable _Finished_ — Optional. The bool indicates the end of the transcription.
- **languageCode** `string` nullable _Languagecode_ — The BCP-47 language code of the transcription.

### LogprobsResult

`object` _LogprobsResult_ — The log probabilities of the tokens generated by the model.

This is useful for understanding the model's confidence in its predictions and
for debugging. For example, you can use log probabilities to identify when the
model is making a less confident prediction or to explore alternative
responses that the model considered. A low log probability can also indicate
that the model is "hallucinating" or generating factually incorrect
information.
- **chosenCandidates** [`LogprobsResultCandidate[]`](#logprobsresultcandidate) nullable _Chosencandidates_ — A list of the chosen candidate tokens at each decoding step. The length of this list is equal to the total number of decoding steps. Note that the chosen candidate might not be in `top_candidates`.
- **topCandidates** [`LogprobsResultTopCandidates[]`](#logprobsresulttopcandidates) nullable _Topcandidates_ — A list of the top candidate tokens at each decoding step. The length of this list is equal to the total number of decoding steps.
- **logProbabilitySum** `number` nullable _Logprobabilitysum_ — Sum of log probabilities for all tokens. This field is not supported in Vertex AI.

### CacheMetadata

`object` _CacheMetadata_ — Metadata for context cache associated with LLM responses.

This class stores cache identification, usage tracking, and lifecycle
information for a particular cache instance. It can be in two states:

1. Active cache state: cache_name is set, all fields populated
2. Fingerprint-only state: cache_name is None, only fingerprint and
   contents_count are set for prefix matching

Token counts (cached and total) are available in the LlmResponse.usage_metadata
and should be accessed from there to avoid duplication.

Attributes:
    cache_name: The full resource name of the cached content (e.g.,
        'projects/123/locations/us-central1/cachedContents/456').
        None when no active cache exists (fingerprint-only state).
    expire_time: Unix timestamp when the cache expires. None when no
        active cache exists.
    fingerprint: Hash of cacheable contents (instruction + tools + contents).
        Always present for prefix matching.
    invocations_used: Number of invocations this cache has been used for.
        None when no active cache exists.
    contents_count: Number of contents. When active cache exists, this is
        the count of cached contents. When no active cache exists, this is
        the total count of contents in the request.
    created_at: Unix timestamp when the cache was created. None when
        no active cache exists.
- **cache_name** `string` nullable _Cache Name_ — Full resource name of the cached content (None if no active cache)
- **expire_time** `number` nullable _Expire Time_ — Unix timestamp when cache expires (None if no active cache)
- **fingerprint** `string` required _Fingerprint_ — Hash of cacheable contents used to detect changes
- **invocations_used** `integer` nullable (minimum: 0) _Invocations Used_ — Number of invocations this cache has been used for (None if no active cache)
- **contents_count** `integer` required (minimum: 0) _Contents Count_ — Number of contents (cached contents when active cache exists, total contents in request when no active cache)
- **created_at** `number` nullable _Created At_ — Unix timestamp when cache was created (None if no active cache)

### CitationMetadata

`object` _CitationMetadata_ — Citation information when the model quotes another source.
- **citations** [`Citation[]`](#citation) nullable _Citations_ — Contains citation information when the model directly quotes, at
      length, from another source. Can include traditional websites and code
      repositories.

### EventActionsOutput

`object` _EventActions_ — Represents the actions attached to an event.
- **skipSummarization** `boolean` nullable _Skipsummarization_
- **stateDelta** `object` _Statedelta_
- **artifactDelta** `object` _Artifactdelta_
  - **«additional properties»** `integer`
- **transferToAgent** `string` nullable _Transfertoagent_
- **escalate** `boolean` nullable _Escalate_
- **requestedAuthConfigs** `object` _Requestedauthconfigs_
  - **«additional properties»** [`GoogleAdkAuthAuthToolAuthConfig`](#googleadkauthauthtoolauthconfig) _AuthConfig_ — The auth config sent by tool asking client to collect auth credentials and

adk and client will help to fill in the response
- **requestedToolConfirmations** `object` _Requestedtoolconfirmations_
  - **«additional properties»** [`ToolConfirmation`](#toolconfirmation) _ToolConfirmation_ — Represents a tool confirmation configuration.
- **compaction** [`EventCompaction`](#eventcompaction) _EventCompaction_ — The compaction of the events.
- **endOfAgent** `boolean` nullable _Endofagent_
- **agentState** `object` nullable _Agentstate_
- **rewindBeforeInvocationId** `string` nullable _Rewindbeforeinvocationid_
- **route** `union` nullable _Route_
  - `boolean`
  - `integer`
  - `string`
  - `union[]`
    - `boolean`
    - `integer`
    - `string`
- **renderUiWidgets** [`UiWidget[]`](#uiwidget) nullable _Renderuiwidgets_
- **setModelResponse** `unknown` nullable _Setmodelresponse_

### NodeInfo

`object` _NodeInfo_ — Workflow node metadata attached to an Event.
- **path** `string` (default: ``) _Path_
- **outputFor** `string[]` nullable _Outputfor_
- **messageAsOutput** `boolean` nullable _Messageasoutput_

### Part

`object` _Part_ — A datatype containing media content.

Exactly one field within a Part should be set, representing the specific type
of content being conveyed. Using multiple fields within the same `Part`
instance is considered invalid.
- **mediaResolution** [`PartMediaResolution`](#partmediaresolution) _PartMediaResolution_ — Media resolution for the input media.
- **codeExecutionResult** [`CodeExecutionResult`](#codeexecutionresult) _CodeExecutionResult_ — Result of executing the ExecutableCode.

Generated only when the `CodeExecution` tool is used.
- **executableCode** [`ExecutableCode`](#executablecode) _ExecutableCode_ — Code generated by the model that is meant to be executed, and the result returned to the model.

Generated when using the `CodeExecution` tool, in which the code will be
automatically executed, and a corresponding CodeExecutionResult will also be
generated.
- **fileData** [`FileData`](#filedata) _FileData_ — URI-based data.

A FileData message contains a URI pointing to data of a specific media type.
It is used to represent images, audio, and video stored in Google Cloud
Storage.
- **functionCall** [`FunctionCall`](#functioncall) _FunctionCall_ — A predicted FunctionCall returned from the model that contains a string representing the FunctionDeclaration.name and a structured JSON object containing the parameters and their values.
- **functionResponse** [`FunctionResponse`](#functionresponse) _FunctionResponse_ — The result output from a FunctionCall that contains a string representing the FunctionDeclaration.name and a structured JSON object containing any output from the function is used as context to the model.

This should contain the result of a `FunctionCall` made based on model
prediction.
- **inlineData** [`Blob`](#blob) _Blob_ — A content blob.

A Blob contains data of a specific media type. It is used to represent images,
audio, and video.
- **text** `string` nullable _Text_ — Optional. The text content of the part. When sent from the VSCode Gemini Code Assist extension, references to @mentioned items will be converted to markdown boldface text. For example `@my-repo` will be converted to and sent as `**my-repo**` by the IDE agent.
- **thought** `boolean` nullable _Thought_ — Optional. Indicates whether the `part` represents the model's thought process or reasoning.
- **thoughtSignature** `string` binary nullable _Thoughtsignature_ — Optional. An opaque signature for the thought so it can be reused in subsequent requests.
- **videoMetadata** [`VideoMetadata`](#videometadata) _VideoMetadata_ — Provides metadata for a video, including the start and end offsets for clipping and the frame rate.
- **toolCall** [`ToolCall`](#toolcall) _ToolCall_ — A predicted server-side `ToolCall` returned from the model.

This message contains information about a tool that the model wants to invoke.
The client is NOT expected to execute this `ToolCall`. Instead, the
client should pass this `ToolCall` back to the API in a subsequent turn
within a `Content` message, along with the corresponding `ToolResponse`.
- **toolResponse** [`ToolResponse`](#toolresponse) _ToolResponse_ — The output from a server-side `ToolCall` execution.

This message contains the results of a tool invocation that was initiated by a
`ToolCall` from the model. The client should pass this `ToolResponse` back to
the API in a subsequent turn within a `Content` message, along with the
corresponding `ToolCall`.
- **partMetadata** `object` nullable _Partmetadata_ — Custom metadata associated with the Part. Agents using genai.Part as content representation may need to keep track of the additional information. For example it can be name of a file/source from which the Part originates or a way to multiplex multiple Part streams. This field is not supported in Vertex AI.

### GroundingChunk

`object` _GroundingChunk_ — A piece of evidence that supports a claim made by the model.

This is used to show a citation for a claim made by the model. When grounding
is enabled, the model returns a `GroundingChunk` that contains a reference to
the source of the information.
- **image** [`GroundingChunkImage`](#groundingchunkimage) _GroundingChunkImage_ — An `Image` chunk is a piece of evidence that comes from an image search result.

It contains the URI of the image search result and the URI of the image. This
is used to provide the user with a link to the source of the information.
- **maps** [`GroundingChunkMaps`](#groundingchunkmaps) _GroundingChunkMaps_ — A `Maps` chunk is a piece of evidence that comes from Google Maps.

It contains information about a place, such as its name, address, and reviews.
This is used to provide the user with rich, location-based information.
- **retrievedContext** [`GroundingChunkRetrievedContext`](#groundingchunkretrievedcontext) _GroundingChunkRetrievedContext_ — Context retrieved from a data source to ground the model's response.

This is used when a retrieval tool fetches information from a user-provided
corpus or a public dataset.
- **web** [`GroundingChunkWeb`](#groundingchunkweb) _GroundingChunkWeb_ — A `Web` chunk is a piece of evidence that comes from a web page.

It contains the URI of the web page, the title of the page, and the domain of
the page. This is used to provide the user with a link to the source of the
information.

### GroundingSupport

`object` _GroundingSupport_ — Grounding support.
- **confidenceScores** `number[]` nullable _Confidencescores_ — Confidence score of the support references.

      Ranges from 0 to 1. 1 is the most confident. This list must have the
      same size as the grounding_chunk_indices.
- **groundingChunkIndices** `integer[]` nullable _Groundingchunkindices_ — A list of indices (into 'grounding_chunk') specifying the
      citations associated with the claim. For instance [1,3,4] means that
      grounding_chunk[1], grounding_chunk[3], grounding_chunk[4] are the
      retrieved content attributed to the claim.
- **segment** [`Segment`](#segment) _Segment_ — Segment of the content this support belongs to.
- **renderedParts** `integer[]` nullable _Renderedparts_ — Indices into the `rendered_parts` field of the `GroundingMetadata` message. These indices specify which rendered parts are associated with this support message.

### RetrievalMetadata

`object` _RetrievalMetadata_ — Metadata returned to client when grounding is enabled.
- **googleSearchDynamicRetrievalScore** `number` nullable _Googlesearchdynamicretrievalscore_ — Optional. Score indicating how likely information from google
      search could help answer the prompt. The score is in the range [0, 1],
      where 0 is the least likely and 1 is the most likely. This score is only
      populated when google search grounding and dynamic retrieval is enabled.
      It will be compared to the threshold to determine whether to trigger
      Google search.

### SearchEntryPoint

`object` _SearchEntryPoint_ — The entry point used to search for grounding sources.
- **renderedContent** `string` nullable _Renderedcontent_ — Optional. Web content snippet that can be embedded in a web page
      or an app webview.
- **sdkBlob** `string` binary nullable _Sdkblob_ — Optional. JSON representing array of tuples.

### GroundingMetadataSourceFlaggingUri

`object` _GroundingMetadataSourceFlaggingUri_ — A URI that can be used to flag a place or review for inappropriate content.

This is populated only when the grounding source is Google Maps. This data
type is not supported in Gemini API.
- **flagContentUri** `string` nullable _Flagcontenturi_ — The URI that can be used to flag the content.
- **sourceId** `string` nullable _Sourceid_ — The ID of the place or review.

### ModalityTokenCount

`object` _ModalityTokenCount_ — Represents a breakdown of token usage by modality.

This message is used in CountTokensResponse and
GenerateContentResponse.UsageMetadata to provide a detailed view of how many
tokens are used by each modality (e.g., text, image, video) in a request. This
is particularly useful for multimodal models, allowing you to track and manage
token consumption for billing and quota purposes.
- **modality** [`MediaModality`](#mediamodality) (`MODALITY_UNSPECIFIED`, `TEXT`, `IMAGE`, `VIDEO`, `AUDIO`, `DOCUMENT`) _MediaModality_ — The modality that this token count applies to.
- **tokenCount** `integer` nullable _Tokencount_ — The number of tokens counted for this modality.

### TrafficType

`string` (`TRAFFIC_TYPE_UNSPECIFIED`, `ON_DEMAND`, `ON_DEMAND_PRIORITY`, `ON_DEMAND_FLEX`, `PROVISIONED_THROUGHPUT`) _TrafficType_ — Output only.

The traffic type for this request. This enum is not supported in Gemini API.

### LogprobsResultCandidate

`object` _LogprobsResultCandidate_ — A single token and its associated log probability.
- **logProbability** `number` nullable _Logprobability_ — The log probability of this token. A higher value indicates that the model was more confident in this token. The log probability can be used to assess the relative likelihood of different tokens and to identify when the model is uncertain.
- **token** `string` nullable _Token_ — The token's string representation.
- **tokenId** `integer` nullable _Tokenid_ — The token's numerical ID. While the `token` field provides the string representation of the token, the `token_id` is the numerical representation that the model uses internally. This can be useful for developers who want to build custom logic based on the model's vocabulary.

### LogprobsResultTopCandidates

`object` _LogprobsResultTopCandidates_ — A list of the top candidate tokens and their log probabilities at each decoding step.

This can be used to see what other tokens the model considered.
- **candidates** [`LogprobsResultCandidate[]`](#logprobsresultcandidate) nullable _Candidates_ — The list of candidate tokens, sorted by log probability in descending order.

### Citation

`object` _Citation_ — A citation for a piece of generatedcontent.

This data type is not supported in Gemini API.
- **endIndex** `integer` nullable _Endindex_ — Output only. The end index of the citation in the content.
- **license** `string` nullable _License_ — Output only. The license of the source of the citation.
- **publicationDate** [`GoogleTypeDate`](#googletypedate) _GoogleTypeDate_ — Represents a whole or partial calendar date, such as a birthday.

The time of day and time zone are either specified elsewhere or are
insignificant. The date is relative to the Gregorian Calendar. This can
represent one of the following: * A full date, with non-zero year, month, and
day values. * A month and day, with a zero year (for example, an anniversary).
* A year on its own, with a zero month and a zero day. * A year and month,
with a zero day (for example, a credit card expiration date). Related types: *
google.type.TimeOfDay * google.type.DateTime * google.protobuf.Timestamp. This
data type is not supported in Gemini API.
- **startIndex** `integer` nullable _Startindex_ — Output only. The start index of the citation in the content.
- **title** `string` nullable _Title_ — Output only. The title of the source of the citation.
- **uri** `string` nullable _Uri_ — Output only. The URI of the source of the citation.

### GoogleAdkAuthAuthToolAuthConfig

`object` _AuthConfig_ — The auth config sent by tool asking client to collect auth credentials and

adk and client will help to fill in the response
- **authScheme** `union` required _Authscheme_
  - [`APIKey`](#apikey) _APIKey_
  - [`HTTPBase`](#httpbase) _HTTPBase_
  - [`OAuth2`](#oauth2) _OAuth2_
  - [`OpenIdConnect`](#openidconnect) _OpenIdConnect_
  - [`HTTPBearer`](#httpbearer) _HTTPBearer_
  - [`OpenIdConnectWithConfig`](#openidconnectwithconfig) _OpenIdConnectWithConfig_
  - [`CustomAuthScheme`](#customauthscheme) _CustomAuthScheme_ — A flexible model for custom authentication schemes.

The subclasses must define a `default` for the `type_` field, if using OAuth2
user consent flow, to ensure correct rehydration.
- **rawAuthCredential** [`AuthCredential`](#authcredential) _AuthCredential_ — Data class representing an authentication credential.

To exchange for the actual credential, please use
CredentialExchanger.exchange_credential().

Examples: API Key Auth
AuthCredential(
    auth_type=AuthCredentialTypes.API_KEY,
    api_key="1234",
)

Example: HTTP Auth
AuthCredential(
    auth_type=AuthCredentialTypes.HTTP,
    http=HttpAuth(
        scheme="basic",
        credentials=HttpCredentials(username="user", password="password"),
    ),
)

Example: OAuth2 Bearer Token in HTTP Header
AuthCredential(
    auth_type=AuthCredentialTypes.HTTP,
    http=HttpAuth(
        scheme="bearer",
        credentials=HttpCredentials(token="eyAkaknabna...."),
    ),
)

Example: OAuth2 Auth with Authorization Code Flow
AuthCredential(
    auth_type=AuthCredentialTypes.OAUTH2,
    oauth2=OAuth2Auth(
        client_id="1234",
        client_secret="secret",
    ),
)

Example: OpenID Connect Auth
AuthCredential(
    auth_type=AuthCredentialTypes.OPEN_ID_CONNECT,
    oauth2=OAuth2Auth(
        client_id="1234",
        client_secret="secret",
        redirect_uri="https://example.com",
        scopes=["scope1", "scope2"],
    ),
)

Example: Auth with resource reference
AuthCredential(
    auth_type=AuthCredentialTypes.API_KEY,
    resource_ref="projects/1234/locations/us-central1/resources/resource1",
)
- **exchangedAuthCredential** [`AuthCredential`](#authcredential) _AuthCredential_ — Data class representing an authentication credential.

To exchange for the actual credential, please use
CredentialExchanger.exchange_credential().

Examples: API Key Auth
AuthCredential(
    auth_type=AuthCredentialTypes.API_KEY,
    api_key="1234",
)

Example: HTTP Auth
AuthCredential(
    auth_type=AuthCredentialTypes.HTTP,
    http=HttpAuth(
        scheme="basic",
        credentials=HttpCredentials(username="user", password="password"),
    ),
)

Example: OAuth2 Bearer Token in HTTP Header
AuthCredential(
    auth_type=AuthCredentialTypes.HTTP,
    http=HttpAuth(
        scheme="bearer",
        credentials=HttpCredentials(token="eyAkaknabna...."),
    ),
)

Example: OAuth2 Auth with Authorization Code Flow
AuthCredential(
    auth_type=AuthCredentialTypes.OAUTH2,
    oauth2=OAuth2Auth(
        client_id="1234",
        client_secret="secret",
    ),
)

Example: OpenID Connect Auth
AuthCredential(
    auth_type=AuthCredentialTypes.OPEN_ID_CONNECT,
    oauth2=OAuth2Auth(
        client_id="1234",
        client_secret="secret",
        redirect_uri="https://example.com",
        scopes=["scope1", "scope2"],
    ),
)

Example: Auth with resource reference
AuthCredential(
    auth_type=AuthCredentialTypes.API_KEY,
    resource_ref="projects/1234/locations/us-central1/resources/resource1",
)
- **credentialKey** `string` nullable _Credentialkey_

### ToolConfirmation

`object` _ToolConfirmation_ — Represents a tool confirmation configuration.
- **hint** `string` (default: ``) _Hint_
- **confirmed** `boolean` (default: `false`) _Confirmed_
- **payload** `unknown` nullable _Payload_

### EventCompaction

`object` _EventCompaction_ — The compaction of the events.
- **startTimestamp** `number` required _Starttimestamp_
- **endTimestamp** `number` required _Endtimestamp_
- **compactedContent** [`Content`](#content) required _Content_ — Contains the multi-part content of a message.

### UiWidget

`object` _UiWidget_ — Rendering metadata for a UI widget associated with an event.

When present on an Event.actions, the UI renders the widget using the
specified provider's renderer component.
- **id** `string` required _Id_
- **provider** `string` required _Provider_
- **payload** `object` _Payload_

### PartMediaResolution

`object` _PartMediaResolution_ — Media resolution for the input media.
- **level** [`PartMediaResolutionLevel`](#partmediaresolutionlevel) (`MEDIA_RESOLUTION_UNSPECIFIED`, `MEDIA_RESOLUTION_LOW`, `MEDIA_RESOLUTION_MEDIUM`, `MEDIA_RESOLUTION_HIGH`, `MEDIA_RESOLUTION_ULTRA_HIGH`) _PartMediaResolutionLevel_ — The tokenization quality used for given media.
- **numTokens** `integer` nullable _Numtokens_ — Specifies the required sequence length for media tokenization.

### CodeExecutionResult

`object` _CodeExecutionResult_ — Result of executing the ExecutableCode.

Generated only when the `CodeExecution` tool is used.
- **outcome** [`Outcome`](#outcome) (`OUTCOME_UNSPECIFIED`, `OUTCOME_OK`, `OUTCOME_FAILED`, `OUTCOME_DEADLINE_EXCEEDED`) _Outcome_ — Outcome of the code execution.
- **output** `string` nullable _Output_ — Optional. Contains stdout when code execution is successful, stderr or other description otherwise.
- **id** `string` nullable _Id_ — Optional. The identifier of the `ExecutableCode` part this result is for. Only populated if the corresponding `ExecutableCode` has an id. This field is not supported in Vertex AI.

### ExecutableCode

`object` _ExecutableCode_ — Code generated by the model that is meant to be executed, and the result returned to the model.

Generated when using the `CodeExecution` tool, in which the code will be
automatically executed, and a corresponding CodeExecutionResult will also be
generated.
- **code** `string` nullable _Code_ — Required. The code to be executed.
- **language** [`Language`](#language) (`LANGUAGE_UNSPECIFIED`, `PYTHON`) _Language_ — Programming language of the `code`.
- **id** `string` nullable _Id_ — Optional. Unique identifier of the `ExecutableCode` part. The server returns the `CodeExecutionResult` with the matching `id`. This field is not supported in Vertex AI.

### FileData

`object` _FileData_ — URI-based data.

A FileData message contains a URI pointing to data of a specific media type.
It is used to represent images, audio, and video stored in Google Cloud
Storage.
- **displayName** `string` nullable _Displayname_ — Optional. The display name of the file. Used to provide a label or filename to distinguish files. This field is only returned in `PromptMessage` for prompt management. It is used in the Gemini calls only when server side tools (`code_execution`, `google_search`, and `url_context`) are enabled. This field is not supported in Gemini API.
- **fileUri** `string` nullable _Fileuri_ — Required. The URI of the file in Google Cloud Storage.
- **mimeType** `string` nullable _Mimetype_ — Required. The IANA standard MIME type of the source data.

### FunctionCall

`object` _FunctionCall_ — A predicted FunctionCall returned from the model that contains a string representing the FunctionDeclaration.name and a structured JSON object containing the parameters and their values.
- **id** `string` nullable _Id_ — Optional. The unique id of the function call. If populated, the client to execute the `function_call` and return the response with the matching `id`.
- **args** `object` nullable _Args_ — Optional. The function parameters and values in JSON object format. See FunctionDeclaration.parameters for parameter details.
- **name** `string` nullable _Name_ — Optional. The name of the function to call. Matches FunctionDeclaration.name.
- **partialArgs** [`PartialArg[]`](#partialarg) nullable _Partialargs_ — Optional. The partial argument value of the function call. If provided, represents the arguments/fields that are streamed incrementally. This field is not supported in Gemini API.
- **willContinue** `boolean` nullable _Willcontinue_ — Optional. Whether this is the last part of the FunctionCall. If true, another partial message for the current FunctionCall is expected to follow. This field is not supported in Gemini API.

### FunctionResponse

`object` _FunctionResponse_ — The result output from a FunctionCall that contains a string representing the FunctionDeclaration.name and a structured JSON object containing any output from the function is used as context to the model.

This should contain the result of a `FunctionCall` made based on model
prediction.
- **willContinue** `boolean` nullable _Willcontinue_ — Optional. Signals that function call continues, and more responses will be returned, turning the function call into a generator. Is only applicable to NON_BLOCKING function calls, is ignored otherwise. If set to false, future responses will not be considered. It is allowed to return empty `response` with `will_continue=False` to signal that the function call is finished. This may still trigger the model generation. To avoid triggering the generation and finish the function call, additionally set `scheduling` to `SILENT`. This field is not supported in Vertex AI.
- **scheduling** [`FunctionResponseScheduling`](#functionresponsescheduling) (`SCHEDULING_UNSPECIFIED`, `SILENT`, `WHEN_IDLE`, `INTERRUPT`) _FunctionResponseScheduling_ — Specifies how the response should be scheduled in the conversation.

Only applicable to NON_BLOCKING function calls, is ignored otherwise. Defaults
to WHEN_IDLE.
- **parts** [`FunctionResponsePart[]`](#functionresponsepart) nullable _Parts_ — Optional. Ordered `Parts` that constitute a function response. Parts may have different IANA MIME types.
- **id** `string` nullable _Id_ — Optional. The id of the function call this response is for. Populated by the client to match the corresponding function call `id`.
- **name** `string` nullable _Name_ — Required. The name of the function to call. Matches FunctionDeclaration.name and FunctionCall.name.
- **response** `object` nullable _Response_ — Required. The function response in JSON object format. Use "output" key to specify function output and "error" key to specify error details (if any). If "output" and "error" keys are not specified, then whole "response" is treated as function output.

### Blob

`object` _Blob_ — A content blob.

A Blob contains data of a specific media type. It is used to represent images,
audio, and video.
- **data** `string` binary nullable _Data_ — Required. The raw bytes of the data.
- **displayName** `string` nullable _Displayname_ — Optional. The display name of the blob. Used to provide a label or filename to distinguish blobs. This field is only returned in `PromptMessage` for prompt management. It is used in the Gemini calls only when server-side tools (`code_execution`, `google_search`, and `url_context`) are enabled. This field is not supported in Gemini API.
- **mimeType** `string` nullable _Mimetype_ — Required. The IANA standard MIME type of the source data.

### VideoMetadata

`object` _VideoMetadata_ — Provides metadata for a video, including the start and end offsets for clipping and the frame rate.
- **endOffset** `string` nullable _Endoffset_ — Optional. The end offset of the video.
- **fps** `number` nullable _Fps_ — Optional. The frame rate of the video sent to the model. If not specified, the default value is 1.0. The valid range is (0.0, 24.0].
- **startOffset** `string` nullable _Startoffset_ — Optional. The start offset of the video.

### ToolCall

`object` _ToolCall_ — A predicted server-side `ToolCall` returned from the model.

This message contains information about a tool that the model wants to invoke.
The client is NOT expected to execute this `ToolCall`. Instead, the
client should pass this `ToolCall` back to the API in a subsequent turn
within a `Content` message, along with the corresponding `ToolResponse`.
- **id** `string` nullable _Id_ — Unique identifier of the tool call. The server returns the tool response with the matching `id`.
- **toolType** [`ToolType`](#tooltype) (`TOOL_TYPE_UNSPECIFIED`, `GOOGLE_SEARCH_WEB`, `GOOGLE_SEARCH_IMAGE`, `URL_CONTEXT`, `GOOGLE_MAPS`, `FILE_SEARCH`) _ToolType_ — The type of tool in the function call.
- **args** `object` nullable _Args_ — The tool call arguments. Example: {"arg1": "value1", "arg2": "value2"}.

### ToolResponse

`object` _ToolResponse_ — The output from a server-side `ToolCall` execution.

This message contains the results of a tool invocation that was initiated by a
`ToolCall` from the model. The client should pass this `ToolResponse` back to
the API in a subsequent turn within a `Content` message, along with the
corresponding `ToolCall`.
- **id** `string` nullable _Id_ — The identifier of the tool call this response is for.
- **toolType** [`ToolType`](#tooltype) (`TOOL_TYPE_UNSPECIFIED`, `GOOGLE_SEARCH_WEB`, `GOOGLE_SEARCH_IMAGE`, `URL_CONTEXT`, `GOOGLE_MAPS`, `FILE_SEARCH`) _ToolType_ — The type of tool in the function call.
- **response** `object` nullable _Response_ — The tool response.

### GroundingChunkImage

`object` _GroundingChunkImage_ — An `Image` chunk is a piece of evidence that comes from an image search result.

It contains the URI of the image search result and the URI of the image. This
is used to provide the user with a link to the source of the information.
- **sourceUri** `string` nullable _Sourceuri_ — The URI of the image search result page.
- **imageUri** `string` nullable _Imageuri_ — The URI of the image.
- **title** `string` nullable _Title_ — The title of the image search result page.
- **domain** `string` nullable _Domain_ — The domain of the image search result page.

### GroundingChunkMaps

`object` _GroundingChunkMaps_ — A `Maps` chunk is a piece of evidence that comes from Google Maps.

It contains information about a place, such as its name, address, and reviews.
This is used to provide the user with rich, location-based information.
- **placeAnswerSources** [`GroundingChunkMapsPlaceAnswerSources`](#groundingchunkmapsplaceanswersources) _GroundingChunkMapsPlaceAnswerSources_ — The sources that were used to generate the place answer.

This includes review snippets and photos that were used to generate the
answer, as well as URIs to flag content.
- **placeId** `string` nullable _Placeid_ — This Place's resource name, in `places/{place_id}` format.

      This can be used to look up the place in the Google Maps API.
- **text** `string` nullable _Text_ — The text of the place answer.
- **title** `string` nullable _Title_ — The title of the place.
- **uri** `string` nullable _Uri_ — The URI of the place.
- **route** [`GroundingChunkMapsRoute`](#groundingchunkmapsroute) _GroundingChunkMapsRoute_ — Route information from Google Maps.

This data type is not supported in Gemini API.

### GroundingChunkRetrievedContext

`object` _GroundingChunkRetrievedContext_ — Context retrieved from a data source to ground the model's response.

This is used when a retrieval tool fetches information from a user-provided
corpus or a public dataset.
- **documentName** `string` nullable _Documentname_ — Output only. The full resource name of the referenced Vertex AI Search document. This is used to identify the specific document that was retrieved. The format is `projects/{project}/locations/{location}/collections/{collection}/dataStores/{data_store}/branches/{branch}/documents/{document}`. This field is not supported in Gemini API.
- **ragChunk** [`RagChunk`](#ragchunk) _RagChunk_ — A RagChunk includes the content of a chunk of a RagFile, and associated metadata.

This data type is not supported in Gemini API.
- **text** `string` nullable _Text_ — The content of the retrieved data source.
- **title** `string` nullable _Title_ — The title of the retrieved data source.
- **uri** `string` nullable _Uri_ — The URI of the retrieved data source.
- **customMetadata** [`GroundingChunkCustomMetadata[]`](#groundingchunkcustommetadata) nullable _Custommetadata_ — Optional. User-provided metadata about the retrieved context. This field is not supported in Vertex AI.
- **fileSearchStore** `string` nullable _Filesearchstore_ — Optional. Name of the `FileSearchStore` containing the document. Example: `fileSearchStores/123`. This field is not supported in Vertex AI.
- **pageNumber** `integer` nullable _Pagenumber_ — Optional. Page number of the retrieved context, if applicable. This field is not supported in Vertex AI.
- **mediaId** `string` nullable _Mediaid_ — Optional. The media blob resource name for multimodal file search results. Format: fileSearchStores/{file_search_store_id}/media/{blob_id}. This field is not supported in Vertex AI.

### GroundingChunkWeb

`object` _GroundingChunkWeb_ — A `Web` chunk is a piece of evidence that comes from a web page.

It contains the URI of the web page, the title of the page, and the domain of
the page. This is used to provide the user with a link to the source of the
information.
- **domain** `string` nullable _Domain_ — The domain of the web page that contains the evidence. This can be used to filter out low-quality sources. This field is not supported in Gemini API.
- **title** `string` nullable _Title_ — The title of the web page that contains the evidence.
- **uri** `string` nullable _Uri_ — The URI of the web page that contains the evidence.

### Segment

`object` _Segment_ — Segment of the content this support belongs to.
- **startIndex** `integer` nullable _Startindex_ — Output only. Start index in the given Part, measured in bytes.

      Offset from the start of the Part, inclusive, starting at zero.
- **endIndex** `integer` nullable _Endindex_ — Output only. End index in the given Part, measured in bytes.

      Offset from the start of the Part, exclusive, starting at zero.
- **partIndex** `integer` nullable _Partindex_ — Output only. The index of a Part object within its parent
      Content object.
- **text** `string` nullable _Text_ — Output only. The text corresponding to the segment from the
      response.

### MediaModality

`string` (`MODALITY_UNSPECIFIED`, `TEXT`, `IMAGE`, `VIDEO`, `AUDIO`, `DOCUMENT`) _MediaModality_ — The modality that this token count applies to.

### GoogleTypeDate

`object` _GoogleTypeDate_ — Represents a whole or partial calendar date, such as a birthday.

The time of day and time zone are either specified elsewhere or are
insignificant. The date is relative to the Gregorian Calendar. This can
represent one of the following: * A full date, with non-zero year, month, and
day values. * A month and day, with a zero year (for example, an anniversary).
* A year on its own, with a zero month and a zero day. * A year and month,
with a zero day (for example, a credit card expiration date). Related types: *
google.type.TimeOfDay * google.type.DateTime * google.protobuf.Timestamp. This
data type is not supported in Gemini API.
- **day** `integer` nullable _Day_ — Day of a month. Must be from 1 to 31 and valid for the year and month, or 0 to specify a year by itself or a year and month where the day isn't significant.
- **month** `integer` nullable _Month_ — Month of a year. Must be from 1 to 12, or 0 to specify a year without a month and day.
- **year** `integer` nullable _Year_ — Year of the date. Must be from 1 to 9999, or 0 to specify a date without a year.

### APIKey

`object` _APIKey_
- **type** [`SecuritySchemeType`](#securityschemetype) (`apiKey`, `http`, `oauth2`, `openIdConnect`) _SecuritySchemeType_
- **description** `string` nullable _Description_
- **in** [`APIKeyIn`](#apikeyin) required (`query`, `header`, `cookie`) _APIKeyIn_
- **name** `string` required _Name_

### HTTPBase

`object` _HTTPBase_
- **type** [`SecuritySchemeType`](#securityschemetype) (`apiKey`, `http`, `oauth2`, `openIdConnect`) _SecuritySchemeType_
- **description** `string` nullable _Description_
- **scheme** `string` required _Scheme_

### OAuth2

`object` _OAuth2_
- **type** [`SecuritySchemeType`](#securityschemetype) (`apiKey`, `http`, `oauth2`, `openIdConnect`) _SecuritySchemeType_
- **description** `string` nullable _Description_
- **flows** [`OAuthFlows`](#oauthflows) required _OAuthFlows_

### OpenIdConnect

`object` _OpenIdConnect_
- **type** [`SecuritySchemeType`](#securityschemetype) (`apiKey`, `http`, `oauth2`, `openIdConnect`) _SecuritySchemeType_
- **description** `string` nullable _Description_
- **openIdConnectUrl** `string` required _Openidconnecturl_

### HTTPBearer

`object` _HTTPBearer_
- **type** [`SecuritySchemeType`](#securityschemetype) (`apiKey`, `http`, `oauth2`, `openIdConnect`) _SecuritySchemeType_
- **description** `string` nullable _Description_
- **scheme** `string` (`bearer`) (default: `bearer`) _Scheme_
- **bearerFormat** `string` nullable _Bearerformat_

### OpenIdConnectWithConfig

`object` _OpenIdConnectWithConfig_
- **type** [`SecuritySchemeType`](#securityschemetype) (`apiKey`, `http`, `oauth2`, `openIdConnect`) _SecuritySchemeType_
- **description** `string` nullable _Description_
- **authorization_endpoint** `string` required _Authorization Endpoint_
- **token_endpoint** `string` required _Token Endpoint_
- **userinfo_endpoint** `string` nullable _Userinfo Endpoint_
- **revocation_endpoint** `string` nullable _Revocation Endpoint_
- **token_endpoint_auth_methods_supported** `string[]` nullable _Token Endpoint Auth Methods Supported_
- **grant_types_supported** `string[]` nullable _Grant Types Supported_
- **scopes** `string[]` nullable _Scopes_

### CustomAuthScheme

`object` _CustomAuthScheme_ — A flexible model for custom authentication schemes.

The subclasses must define a `default` for the `type_` field, if using OAuth2
user consent flow, to ensure correct rehydration.
- **type** `string` required _Type_

### AuthCredential

`object` _AuthCredential_ — Data class representing an authentication credential.

To exchange for the actual credential, please use
CredentialExchanger.exchange_credential().

Examples: API Key Auth
AuthCredential(
    auth_type=AuthCredentialTypes.API_KEY,
    api_key="1234",
)

Example: HTTP Auth
AuthCredential(
    auth_type=AuthCredentialTypes.HTTP,
    http=HttpAuth(
        scheme="basic",
        credentials=HttpCredentials(username="user", password="password"),
    ),
)

Example: OAuth2 Bearer Token in HTTP Header
AuthCredential(
    auth_type=AuthCredentialTypes.HTTP,
    http=HttpAuth(
        scheme="bearer",
        credentials=HttpCredentials(token="eyAkaknabna...."),
    ),
)

Example: OAuth2 Auth with Authorization Code Flow
AuthCredential(
    auth_type=AuthCredentialTypes.OAUTH2,
    oauth2=OAuth2Auth(
        client_id="1234",
        client_secret="secret",
    ),
)

Example: OpenID Connect Auth
AuthCredential(
    auth_type=AuthCredentialTypes.OPEN_ID_CONNECT,
    oauth2=OAuth2Auth(
        client_id="1234",
        client_secret="secret",
        redirect_uri="https://example.com",
        scopes=["scope1", "scope2"],
    ),
)

Example: Auth with resource reference
AuthCredential(
    auth_type=AuthCredentialTypes.API_KEY,
    resource_ref="projects/1234/locations/us-central1/resources/resource1",
)
- **authType** [`AuthCredentialTypes`](#authcredentialtypes) required (`apiKey`, `http`, `oauth2`, `openIdConnect`, `serviceAccount`) _AuthCredentialTypes_ — Represents the type of authentication credential.
- **resourceRef** `string` nullable _Resourceref_
- **apiKey** `string` nullable _Apikey_
- **http** [`HttpAuth`](#httpauth) _HttpAuth_ — The credentials and metadata for HTTP authentication.
- **serviceAccount** [`ServiceAccount`](#serviceaccount) _ServiceAccount_ — Represents Google Service Account configuration.

Attributes:
  service_account_credential: The service account credential (JSON key).
  scopes: The OAuth2 scopes to request. Optional; when omitted with
      ``use_default_credential=True``, defaults to the cloud-platform scope.
  use_default_credential: Whether to use Application Default Credentials.
  use_id_token: Whether to exchange for an ID token instead of an access
      token. Required for service-to-service authentication with Cloud Run,
      Cloud Functions, and other Google Cloud services that require identity
      verification. When True, ``audience`` must also be set.
  audience: The target audience for the ID token, typically the URL of the
      receiving service (e.g. ``https://my-service-xyz.run.app``). Required
      when ``use_id_token`` is True.
- **oauth2** [`OAuth2Auth`](#oauth2auth) _OAuth2Auth_ — Represents credential value and its metadata for a OAuth2 credential.

### PartMediaResolutionLevel

`string` (`MEDIA_RESOLUTION_UNSPECIFIED`, `MEDIA_RESOLUTION_LOW`, `MEDIA_RESOLUTION_MEDIUM`, `MEDIA_RESOLUTION_HIGH`, `MEDIA_RESOLUTION_ULTRA_HIGH`) _PartMediaResolutionLevel_ — The tokenization quality used for given media.

### Outcome

`string` (`OUTCOME_UNSPECIFIED`, `OUTCOME_OK`, `OUTCOME_FAILED`, `OUTCOME_DEADLINE_EXCEEDED`) _Outcome_ — Outcome of the code execution.

### Language

`string` (`LANGUAGE_UNSPECIFIED`, `PYTHON`) _Language_ — Programming language of the `code`.

### PartialArg

`object` _PartialArg_ — Partial argument value of the function call.

This data type is not supported in Gemini API.
- **boolValue** `boolean` nullable _Boolvalue_ — Optional. Represents a boolean value.
- **jsonPath** `string` nullable _Jsonpath_ — Required. A JSON Path (RFC 9535) to the argument being streamed. https://datatracker.ietf.org/doc/html/rfc9535. e.g. "$.foo.bar[0].data".
- **nullValue** `string` nullable (`NULL_VALUE`) _Nullvalue_ — Optional. Represents a null value.
- **numberValue** `number` nullable _Numbervalue_ — Optional. Represents a double value.
- **stringValue** `string` nullable _Stringvalue_ — Optional. Represents a string value.
- **willContinue** `boolean` nullable _Willcontinue_ — Optional. Whether this is not the last part of the same json_path. If true, another PartialArg message for the current json_path is expected to follow.

### FunctionResponseScheduling

`string` (`SCHEDULING_UNSPECIFIED`, `SILENT`, `WHEN_IDLE`, `INTERRUPT`) _FunctionResponseScheduling_ — Specifies how the response should be scheduled in the conversation.

Only applicable to NON_BLOCKING function calls, is ignored otherwise. Defaults
to WHEN_IDLE.

### FunctionResponsePart

`object` _FunctionResponsePart_ — A datatype containing media that is part of a `FunctionResponse` message.

A `FunctionResponsePart` consists of data which has an associated datatype. A
`FunctionResponsePart` can only contain one of the accepted types in
`FunctionResponsePart.data`. A `FunctionResponsePart` must have a fixed IANA
MIME type identifying the type and subtype of the media if the `inline_data`
field is filled with raw bytes.
- **inlineData** [`FunctionResponseBlob`](#functionresponseblob) _FunctionResponseBlob_ — Raw media bytes for function response.

Text should not be sent as raw bytes, use the 'text' field.
- **fileData** [`FunctionResponseFileData`](#functionresponsefiledata) _FunctionResponseFileData_ — URI based data for function response.

This data type is not supported in Gemini API.

### ToolType

`string` (`TOOL_TYPE_UNSPECIFIED`, `GOOGLE_SEARCH_WEB`, `GOOGLE_SEARCH_IMAGE`, `URL_CONTEXT`, `GOOGLE_MAPS`, `FILE_SEARCH`) _ToolType_ — The type of tool in the function call.

### GroundingChunkMapsPlaceAnswerSources

`object` _GroundingChunkMapsPlaceAnswerSources_ — The sources that were used to generate the place answer.

This includes review snippets and photos that were used to generate the
answer, as well as URIs to flag content.
- **reviewSnippet** [`GroundingChunkMapsPlaceAnswerSourcesReviewSnippet[]`](#groundingchunkmapsplaceanswersourcesreviewsnippet) nullable _Reviewsnippet_ — Snippets of reviews that were used to generate the answer.
- **flagContentUri** `string` nullable _Flagcontenturi_ — A link where users can flag a problem with the generated answer.
- **reviewSnippets** [`GroundingChunkMapsPlaceAnswerSourcesReviewSnippet[]`](#groundingchunkmapsplaceanswersourcesreviewsnippet) nullable _Reviewsnippets_ — Snippets of reviews that were used to generate the answer.

### GroundingChunkMapsRoute

`object` _GroundingChunkMapsRoute_ — Route information from Google Maps.

This data type is not supported in Gemini API.
- **distanceMeters** `integer` nullable _Distancemeters_ — The total distance of the route, in meters.
- **duration** `string` nullable _Duration_ — The total duration of the route.
- **encodedPolyline** `string` nullable _Encodedpolyline_ — An encoded polyline of the route. See https://developers.google.com/maps/documentation/utilities/polylinealgorithm

### RagChunk

`object` _RagChunk_ — A RagChunk includes the content of a chunk of a RagFile, and associated metadata.

This data type is not supported in Gemini API.
- **pageSpan** [`RagChunkPageSpan`](#ragchunkpagespan) _RagChunkPageSpan_ — Represents where the chunk starts and ends in the document.

This data type is not supported in Gemini API.
- **text** `string` nullable _Text_ — The content of the chunk.
- **chunkId** `string` nullable _Chunkid_ — The ID of the chunk.
- **fileId** `string` nullable _Fileid_ — The ID of the file that the chunk belongs to.

### GroundingChunkCustomMetadata

`object` _GroundingChunkCustomMetadata_ — User provided metadata about the GroundingFact.

This data type is not supported in Vertex AI.
- **key** `string` nullable _Key_ — The key of the metadata.
- **numericValue** `number` nullable _Numericvalue_ — Optional. The numeric value of the metadata. The expected range for this value depends on the specific `key` used.
- **stringListValue** [`GroundingChunkStringList`](#groundingchunkstringlist) _GroundingChunkStringList_ — A list of string values. This data type is not supported in Vertex AI.
- **stringValue** `string` nullable _Stringvalue_ — Optional. The string value of the metadata.

### SecuritySchemeType

`string` (`apiKey`, `http`, `oauth2`, `openIdConnect`) _SecuritySchemeType_

### APIKeyIn

`string` (`query`, `header`, `cookie`) _APIKeyIn_

### OAuthFlows

`object` _OAuthFlows_
- **implicit** [`OAuthFlowImplicit`](#oauthflowimplicit) _OAuthFlowImplicit_
- **password** [`OAuthFlowPassword`](#oauthflowpassword) _OAuthFlowPassword_
- **clientCredentials** [`OAuthFlowClientCredentials`](#oauthflowclientcredentials) _OAuthFlowClientCredentials_
- **authorizationCode** [`OAuthFlowAuthorizationCode`](#oauthflowauthorizationcode) _OAuthFlowAuthorizationCode_

### AuthCredentialTypes

`string` (`apiKey`, `http`, `oauth2`, `openIdConnect`, `serviceAccount`) _AuthCredentialTypes_ — Represents the type of authentication credential.

### HttpAuth

`object` _HttpAuth_ — The credentials and metadata for HTTP authentication.
- **scheme** `string` required _Scheme_
- **credentials** [`HttpCredentials`](#httpcredentials) required _HttpCredentials_ — Represents the secret token value for HTTP authentication, like user name, password, oauth token, etc.
- **additionalHeaders** `object` nullable _Additionalheaders_
  - **«additional properties»** `string`

### ServiceAccount

`object` _ServiceAccount_ — Represents Google Service Account configuration.

Attributes:
  service_account_credential: The service account credential (JSON key).
  scopes: The OAuth2 scopes to request. Optional; when omitted with
      ``use_default_credential=True``, defaults to the cloud-platform scope.
  use_default_credential: Whether to use Application Default Credentials.
  use_id_token: Whether to exchange for an ID token instead of an access
      token. Required for service-to-service authentication with Cloud Run,
      Cloud Functions, and other Google Cloud services that require identity
      verification. When True, ``audience`` must also be set.
  audience: The target audience for the ID token, typically the URL of the
      receiving service (e.g. ``https://my-service-xyz.run.app``). Required
      when ``use_id_token`` is True.
- **serviceAccountCredential** [`ServiceAccountCredential`](#serviceaccountcredential) _ServiceAccountCredential_ — Represents Google Service Account configuration.

Attributes:
  type: The type should be "service_account".
  project_id: The project ID.
  private_key_id: The ID of the private key.
  private_key: The private key.
  client_email: The client email.
  client_id: The client ID.
  auth_uri: The authorization URI.
  token_uri: The token URI.
  auth_provider_x509_cert_url: URL for auth provider's X.509 cert.
  client_x509_cert_url: URL for the client's X.509 cert.
  universe_domain: The universe domain.

Example:

    config = ServiceAccountCredential(
        type_="service_account",
        project_id="your_project_id",
        private_key_id="your_private_key_id",
        private_key="-----BEGIN PRIVATE KEY-----...",
        client_email="...@....iam.gserviceaccount.com",
        client_id="your_client_id",
        auth_uri="https://accounts.google.com/o/oauth2/auth",
        token_uri="https://oauth2.googleapis.com/token",
        auth_provider_x509_cert_url="https://www.googleapis.com/oauth2/v1/certs",
        client_x509_cert_url="https://www.googleapis.com/robot/v1/metadata/x509/...",
        universe_domain="googleapis.com"
    )


    config = ServiceAccountConfig.model_construct(**{
        ...service account config dict
    })
- **scopes** `string[]` nullable _Scopes_
- **useDefaultCredential** `boolean` nullable (default: `false`) _Usedefaultcredential_
- **useIdToken** `boolean` nullable (default: `false`) _Useidtoken_
- **audience** `string` nullable _Audience_

### OAuth2Auth

`object` _OAuth2Auth_ — Represents credential value and its metadata for a OAuth2 credential.
- **clientId** `string` nullable _Clientid_
- **clientSecret** `string` nullable _Clientsecret_
- **authUri** `string` nullable _Authuri_
- **nonce** `string` nullable _Nonce_
- **state** `string` nullable _State_
- **redirectUri** `string` nullable _Redirecturi_
- **authResponseUri** `string` nullable _Authresponseuri_
- **authCode** `string` nullable _Authcode_
- **accessToken** `string` nullable _Accesstoken_
- **refreshToken** `string` nullable _Refreshtoken_
- **idToken** `string` nullable _Idtoken_
- **expiresAt** `integer` nullable _Expiresat_
- **expiresIn** `integer` nullable _Expiresin_
- **audience** `string` nullable _Audience_
- **codeVerifier** `string` nullable _Codeverifier_
- **codeChallengeMethod** `string` nullable _Codechallengemethod_
- **tokenEndpointAuthMethod** `string` nullable (`client_secret_basic`, `client_secret_post`, `client_secret_jwt`, `private_key_jwt`) (default: `client_secret_basic`) _Tokenendpointauthmethod_

### FunctionResponseBlob

`object` _FunctionResponseBlob_ — Raw media bytes for function response.

Text should not be sent as raw bytes, use the 'text' field.
- **mimeType** `string` nullable _Mimetype_ — Required. The IANA standard MIME type of the source data.
- **data** `string` binary nullable _Data_ — Required. Raw bytes.
- **displayName** `string` nullable _Displayname_ — Optional. Display name of the blob. Used to provide a label or filename to distinguish blobs. This field is only returned in PromptMessage for prompt management. It is currently used in the Gemini GenerateContent calls only when server side tools (code_execution, google_search, and url_context) are enabled. This field is not supported in Gemini API.

### FunctionResponseFileData

`object` _FunctionResponseFileData_ — URI based data for function response.

This data type is not supported in Gemini API.
- **fileUri** `string` nullable _Fileuri_ — Required. URI.
- **mimeType** `string` nullable _Mimetype_ — Required. The IANA standard MIME type of the source data.
- **displayName** `string` nullable _Displayname_ — Optional. Display name of the file data. Used to provide a label or filename to distinguish file datas. This field is only returned in PromptMessage for prompt management. It is currently used in the Gemini GenerateContent calls only when server side tools (code_execution, google_search, and url_context) are enabled.

### GroundingChunkMapsPlaceAnswerSourcesReviewSnippet

`object` _GroundingChunkMapsPlaceAnswerSourcesReviewSnippet_ — Encapsulates a review snippet.
- **authorAttribution** [`GroundingChunkMapsPlaceAnswerSourcesAuthorAttribution`](#groundingchunkmapsplaceanswersourcesauthorattribution) _GroundingChunkMapsPlaceAnswerSourcesAuthorAttribution_ — Author attribution for a photo or review.
- **flagContentUri** `string` nullable _Flagcontenturi_ — A link where users can flag a problem with the review.
- **googleMapsUri** `string` nullable _Googlemapsuri_ — A link to show the review on Google Maps.
- **relativePublishTimeDescription** `string` nullable _Relativepublishtimedescription_ — A string of formatted recent time, expressing the review time relative to the current time in a form appropriate for the language and country.
- **review** `string` nullable _Review_ — A reference representing this place review which may be used to look up this place review again.
- **reviewId** `string` nullable _Reviewid_ — Id of the review referencing the place.
- **title** `string` nullable _Title_ — Title of the review.

### RagChunkPageSpan

`object` _RagChunkPageSpan_ — Represents where the chunk starts and ends in the document.

This data type is not supported in Gemini API.
- **firstPage** `integer` nullable _Firstpage_ — Page where chunk starts in the document. Inclusive. 1-indexed.
- **lastPage** `integer` nullable _Lastpage_ — Page where chunk ends in the document. Inclusive. 1-indexed.

### GroundingChunkStringList

`object` _GroundingChunkStringList_ — A list of string values. This data type is not supported in Vertex AI.
- **values** `string[]` nullable _Values_ — The string values of the list.

### OAuthFlowImplicit

`object` _OAuthFlowImplicit_
- **refreshUrl** `string` nullable _Refreshurl_
- **scopes** `object` (default: `[object Object]`) _Scopes_
  - **«additional properties»** `string`
- **authorizationUrl** `string` required _Authorizationurl_

### OAuthFlowPassword

`object` _OAuthFlowPassword_
- **refreshUrl** `string` nullable _Refreshurl_
- **scopes** `object` (default: `[object Object]`) _Scopes_
  - **«additional properties»** `string`
- **tokenUrl** `string` required _Tokenurl_

### OAuthFlowClientCredentials

`object` _OAuthFlowClientCredentials_
- **refreshUrl** `string` nullable _Refreshurl_
- **scopes** `object` (default: `[object Object]`) _Scopes_
  - **«additional properties»** `string`
- **tokenUrl** `string` required _Tokenurl_

### OAuthFlowAuthorizationCode

`object` _OAuthFlowAuthorizationCode_
- **refreshUrl** `string` nullable _Refreshurl_
- **scopes** `object` (default: `[object Object]`) _Scopes_
  - **«additional properties»** `string`
- **authorizationUrl** `string` required _Authorizationurl_
- **tokenUrl** `string` required _Tokenurl_

### HttpCredentials

`object` _HttpCredentials_ — Represents the secret token value for HTTP authentication, like user name, password, oauth token, etc.
- **username** `string` nullable _Username_
- **password** `string` nullable _Password_
- **token** `string` nullable _Token_

### ServiceAccountCredential

`object` _ServiceAccountCredential_ — Represents Google Service Account configuration.

Attributes:
  type: The type should be "service_account".
  project_id: The project ID.
  private_key_id: The ID of the private key.
  private_key: The private key.
  client_email: The client email.
  client_id: The client ID.
  auth_uri: The authorization URI.
  token_uri: The token URI.
  auth_provider_x509_cert_url: URL for auth provider's X.509 cert.
  client_x509_cert_url: URL for the client's X.509 cert.
  universe_domain: The universe domain.

Example:

    config = ServiceAccountCredential(
        type_="service_account",
        project_id="your_project_id",
        private_key_id="your_private_key_id",
        private_key="-----BEGIN PRIVATE KEY-----...",
        client_email="...@....iam.gserviceaccount.com",
        client_id="your_client_id",
        auth_uri="https://accounts.google.com/o/oauth2/auth",
        token_uri="https://oauth2.googleapis.com/token",
        auth_provider_x509_cert_url="https://www.googleapis.com/oauth2/v1/certs",
        client_x509_cert_url="https://www.googleapis.com/robot/v1/metadata/x509/...",
        universe_domain="googleapis.com"
    )


    config = ServiceAccountConfig.model_construct(**{
        ...service account config dict
    })
- **type** `string` (default: ``) _Type_
- **projectId** `string` required _Projectid_
- **privateKeyId** `string` required _Privatekeyid_
- **privateKey** `string` required _Privatekey_
- **clientEmail** `string` required _Clientemail_
- **clientId** `string` required _Clientid_
- **authUri** `string` required _Authuri_
- **tokenUri** `string` required _Tokenuri_
- **authProviderX509CertUrl** `string` required _Authproviderx509Certurl_
- **clientX509CertUrl** `string` required _Clientx509Certurl_
- **universeDomain** `string` required _Universedomain_

### GroundingChunkMapsPlaceAnswerSourcesAuthorAttribution

`object` _GroundingChunkMapsPlaceAnswerSourcesAuthorAttribution_ — Author attribution for a photo or review.
- **displayName** `string` nullable _Displayname_ — Name of the author of the Photo or Review.
- **photoUri** `string` nullable _Photouri_ — Profile photo URI of the author of the Photo or Review.
- **uri** `string` nullable _Uri_ — URI of the author of the Photo or Review.