---
type: operation
title: "List Apps"
operationId: "list_apps_list_apps_get"
method: GET
path: "/list-apps"
---

# List Apps

`GET` `/list-apps`

## Query parameters

**detailed** `boolean` (default: `false`) _Detailed_ — Return detailed app information

## Responses

### `200` — Successful Response

`union` _Response List Apps List Apps Get_
- `string[]`
- [`ListAppsResponse`](#listappsresponse) _ListAppsResponse_

### `422` — Validation Error

[`HTTPValidationError`](#httpvalidationerror) _HTTPValidationError_

## Referenced types

### ListAppsResponse

`object` _ListAppsResponse_
- **apps** [`AppInfo[]`](#appinfo) required _Apps_

### HTTPValidationError

`object` _HTTPValidationError_
- **detail** [`ValidationError[]`](#validationerror) _Detail_

### AppInfo

`object` _AppInfo_
- **name** `string` required _Name_
- **rootAgentName** `string` required _Rootagentname_
- **description** `string` required _Description_
- **language** `string` required (`yaml`, `python`) _Language_
- **isComputerUse** `boolean` (default: `false`) _Iscomputeruse_
- **agents** `object` nullable _Agents_
  - **«additional properties»** [`AgentInfo`](#agentinfo) _AgentInfo_

### ValidationError

`object` _ValidationError_
- **loc** `union[]` required _Location_
  - `string`
  - `integer`
- **msg** `string` required _Message_
- **type** `string` required _Error Type_
- **input** `unknown` _Input_
- **ctx** `object` _Context_

### AgentInfo

`object` _AgentInfo_
- **name** `string` required _Name_
- **description** `string` required _Description_
- **instruction** `string` required _Instruction_
- **tools** [`Tool[]`](#tool) required _Tools_
- **sub_agents** `string[]` required _Sub Agents_

### Tool

`object` _Tool_ — Tool details of a tool that the model may use to generate a response.
- **retrieval** [`Retrieval`](#retrieval) _Retrieval_ — Defines a retrieval tool that model can call to access external knowledge.

This data type is not supported in Gemini API.
- **computerUse** [`ComputerUse`](#computeruse) _ComputerUse_ — Tool to support computer use.
- **fileSearch** [`FileSearch`](#filesearch) _FileSearch_ — The FileSearch tool that retrieves knowledge from Semantic Retrieval corpora.

Files are imported to Semantic Retrieval corpora using the ImportFile API.
This data type is not supported in Vertex AI.
- **googleSearch** [`GoogleSearch`](#googlesearch) _GoogleSearch_ — GoogleSearch tool type.

Tool to support Google Search in Model. Powered by Google.
- **googleMaps** [`GoogleMaps`](#googlemaps) _GoogleMaps_ — Tool to retrieve knowledge from Google Maps.
- **codeExecution** [`ToolCodeExecution`](#toolcodeexecution) _ToolCodeExecution_ — Tool that executes code generated by the model, and automatically returns the result to the model.

See also ExecutableCode and CodeExecutionResult, which are input and output to
this tool. This data type is not supported in Gemini API.
- **enterpriseWebSearch** [`EnterpriseWebSearch`](#enterprisewebsearch) _EnterpriseWebSearch_ — Tool to search public web data, powered by Vertex AI Search and Sec4 compliance.

This data type is not supported in Gemini API.
- **functionDeclarations** [`FunctionDeclaration[]`](#functiondeclaration) nullable _Functiondeclarations_ — Optional. Function tool type. One or more function declarations to be passed to the model along with the current user query. Model may decide to call a subset of these functions by populating FunctionCall in the response. User should provide a FunctionResponse for each function call in the next turn. Based on the function responses, Model will generate the final response back to the user. Maximum 512 function declarations can be provided.
- **googleSearchRetrieval** [`GoogleSearchRetrieval`](#googlesearchretrieval) _GoogleSearchRetrieval_ — Tool to retrieve public web data for grounding, powered by Google.
- **parallelAiSearch** [`ToolParallelAiSearch`](#toolparallelaisearch) _ToolParallelAiSearch_ — ParallelAiSearch tool type.

A tool that uses the Parallel.ai search engine for grounding. This data type
is not supported in Gemini API.
- **urlContext** [`UrlContext`](#urlcontext) _UrlContext_ — Tool to support URL context.
- **mcpServers** [`McpServer[]`](#mcpserver) nullable _Mcpservers_ — Optional. MCP Servers to connect to. This field is not supported in Vertex AI.

### Retrieval

`object` _Retrieval_ — Defines a retrieval tool that model can call to access external knowledge.

This data type is not supported in Gemini API.
- **disableAttribution** `boolean` nullable _Disableattribution_ — Optional. Deprecated. This option is no longer supported.
- **externalApi** [`ExternalApi`](#externalapi) _ExternalApi_ — Retrieve from data source powered by external API for grounding.

The external API is not owned by Google, but need to follow the pre-defined
API spec. This data type is not supported in Gemini API.
- **vertexAiSearch** [`VertexAISearch`](#vertexaisearch) _VertexAISearch_ — Retrieve from Vertex AI Search datastore or engine for grounding.

datastore and engine are mutually exclusive. See
https://cloud.google.com/products/agent-builder. This data type is not
supported in Gemini API.
- **vertexRagStore** [`VertexRagStore`](#vertexragstore) _VertexRagStore_ — Retrieve from Vertex RAG Store for grounding.

This data type is not supported in Gemini API.

### ComputerUse

`object` _ComputerUse_ — Tool to support computer use.
- **environment** [`Environment`](#environment) (`ENVIRONMENT_UNSPECIFIED`, `ENVIRONMENT_BROWSER`, `ENVIRONMENT_MOBILE`, `ENVIRONMENT_DESKTOP`) _Environment_ — The environment being operated.
- **excludedPredefinedFunctions** `string[]` nullable _Excludedpredefinedfunctions_ — Optional. By default, [predefined functions](https://cloud.google.com/vertex-ai/generative-ai/docs/computer-use#supported-actions) are included in the final model call. Some of them can be explicitly excluded from being automatically included. This can serve two purposes: 1. Using a more restricted / different action space. 2. Improving the definitions / instructions of predefined functions.
- **enablePromptInjectionDetection** `boolean` nullable _Enablepromptinjectiondetection_ — Optional. Enables the prompt injection detection check on computer-use request.
- **disabledSafetyPolicies** [`SafetyPolicy[]`](#safetypolicy) nullable _Disabledsafetypolicies_ — Optional. Disabled safety policies for computer use. This field is not supported in Vertex AI.

### FileSearch

`object` _FileSearch_ — The FileSearch tool that retrieves knowledge from Semantic Retrieval corpora.

Files are imported to Semantic Retrieval corpora using the ImportFile API.
This data type is not supported in Vertex AI.
- **fileSearchStoreNames** `string[]` nullable _Filesearchstorenames_ — Required. The names of the file_search_stores to retrieve from. Example: `fileSearchStores/my-file-search-store-123`
- **topK** `integer` nullable _Topk_ — Optional. The number of semantic retrieval chunks to retrieve.
- **metadataFilter** `string` nullable _Metadatafilter_ — Optional. Metadata filter to apply to the semantic retrieval documents and chunks.

### GoogleSearch

`object` _GoogleSearch_ — GoogleSearch tool type.

Tool to support Google Search in Model. Powered by Google.
- **searchTypes** [`SearchTypes`](#searchtypes) _SearchTypes_ — Different types of search that can be enabled on the GoogleSearch tool.
- **blockingConfidence** [`PhishBlockThreshold`](#phishblockthreshold) (`PHISH_BLOCK_THRESHOLD_UNSPECIFIED`, `BLOCK_LOW_AND_ABOVE`, `BLOCK_MEDIUM_AND_ABOVE`, `BLOCK_HIGH_AND_ABOVE`, `BLOCK_HIGHER_AND_ABOVE`, `BLOCK_VERY_HIGH_AND_ABOVE`, `BLOCK_ONLY_EXTREMELY_HIGH`) _PhishBlockThreshold_ — Sites with confidence level chosen & above this value will be blocked from the search results.

This enum is not supported in Gemini API.
- **excludeDomains** `string[]` nullable _Excludedomains_ — Optional. List of domains to be excluded from the search results. The default limit is 2000 domains. Example: ["amazon.com", "facebook.com"]. This field is not supported in Gemini API.
- **timeRangeFilter** [`Interval`](#interval) _Interval_ — Represents a time interval, encoded as a Timestamp start (inclusive) and a Timestamp end (exclusive).

The start must be less than or equal to the end. When the start equals the
end, the interval is empty (matches no time). When both start and end are
unspecified, the interval matches any time.

### GoogleMaps

`object` _GoogleMaps_ — Tool to retrieve knowledge from Google Maps.
- **authConfig** [`GoogleGenaiTypesAuthConfig`](#googlegenaitypesauthconfig) _AuthConfig_ — The authentication config to access the API.
- **enableWidget** `boolean` nullable _Enablewidget_ — Deprecated. The Google Maps contextual widget behavior in Grounding with Google Maps is being deprecated; this field is planned for removal and no longer has any effect once removed. Optional. Whether to return a widget context token in the GroundingMetadata of the response.

### ToolCodeExecution

`object` _ToolCodeExecution_ — Tool that executes code generated by the model, and automatically returns the result to the model.

See also ExecutableCode and CodeExecutionResult, which are input and output to
this tool. This data type is not supported in Gemini API.

### EnterpriseWebSearch

`object` _EnterpriseWebSearch_ — Tool to search public web data, powered by Vertex AI Search and Sec4 compliance.

This data type is not supported in Gemini API.
- **blockingConfidence** [`PhishBlockThreshold`](#phishblockthreshold) (`PHISH_BLOCK_THRESHOLD_UNSPECIFIED`, `BLOCK_LOW_AND_ABOVE`, `BLOCK_MEDIUM_AND_ABOVE`, `BLOCK_HIGH_AND_ABOVE`, `BLOCK_HIGHER_AND_ABOVE`, `BLOCK_VERY_HIGH_AND_ABOVE`, `BLOCK_ONLY_EXTREMELY_HIGH`) _PhishBlockThreshold_ — Sites with confidence level chosen & above this value will be blocked from the search results.

This enum is not supported in Gemini API.
- **excludeDomains** `string[]` nullable _Excludedomains_ — Optional. List of domains to be excluded from the search results. The default limit is 2000 domains.

### FunctionDeclaration

`object` _FunctionDeclaration_ — Structured representation of a function declaration as defined by the [OpenAPI 3.0 specification](https://spec.openapis.org/oas/v3.0.3).

Included in this declaration are the function name, description, parameters
and response type. This FunctionDeclaration is a representation of a block of
code that can be used as a `Tool` by the model and executed by the client.
- **description** `string` nullable _Description_ — Optional. Description and purpose of the function. Model uses it to decide how and whether to call the function.
- **name** `string` nullable _Name_ — Required. The name of the function to call. Must start with a letter or an underscore. Must be a-z, A-Z, 0-9, or contain underscores, dots, colons and dashes, with a maximum length of 128.
- **parameters** [`Schema`](#schema) _Schema_ — Schema is used to define the format of input/output data.

Represents a select subset of an [OpenAPI 3.0 schema
object](https://spec.openapis.org/oas/v3.0.3#schema-object). More fields may
be added in the future as needed.
- **parametersJsonSchema** `unknown` nullable _Parametersjsonschema_ — Optional. Describes the parameters to the function in JSON Schema format. The schema must describe an object where the properties are the parameters to the function. For example: ``` { "type": "object", "properties": { "name": { "type": "string" }, "age": { "type": "integer" } }, "additionalProperties": false, "required": ["name", "age"], "propertyOrdering": ["name", "age"] } ``` This field is mutually exclusive with `parameters`.
- **response** [`Schema`](#schema) _Schema_ — Schema is used to define the format of input/output data.

Represents a select subset of an [OpenAPI 3.0 schema
object](https://spec.openapis.org/oas/v3.0.3#schema-object). More fields may
be added in the future as needed.
- **responseJsonSchema** `unknown` nullable _Responsejsonschema_ — Optional. Describes the output from this function in JSON Schema format. The value specified by the schema is the response value of the function. This field is mutually exclusive with `response`.
- **behavior** [`Behavior`](#behavior) (`UNSPECIFIED`, `BLOCKING`, `NON_BLOCKING`) _Behavior_ — Specifies the function Behavior.

Currently only non-blocking functions are supported. If not specified, the
system keeps the current function call behavior. This field is currently only
supported by the BidiGenerateContent method.

### GoogleSearchRetrieval

`object` _GoogleSearchRetrieval_ — Tool to retrieve public web data for grounding, powered by Google.
- **dynamicRetrievalConfig** [`DynamicRetrievalConfig`](#dynamicretrievalconfig) _DynamicRetrievalConfig_ — Describes the options to customize dynamic retrieval.

### ToolParallelAiSearch

`object` _ToolParallelAiSearch_ — ParallelAiSearch tool type.

A tool that uses the Parallel.ai search engine for grounding. This data type
is not supported in Gemini API.
- **apiKey** `string` nullable _Apikey_ — Optional. The API key for ParallelAiSearch. If an API key is not provided, the system will attempt to verify access by checking for an active Parallel.ai subscription through the Google Cloud Marketplace. See https://docs.parallel.ai/search/search-quickstart for more details.
- **customConfigs** `object` nullable _Customconfigs_ — Optional. Custom configs for ParallelAiSearch. This field can be used to pass any parameter from the Parallel.ai Search API. See the Parallel.ai documentation for the full list of available parameters and their usage: https://docs.parallel.ai/api-reference/search-beta/search Currently only `source_policy`, `excerpts`, `max_results`, `mode`, `fetch_policy` can be set via this field. For example: { "source_policy": { "include_domains": ["google.com", "wikipedia.org"], "exclude_domains": ["example.com"] }, "fetch_policy": { "max_age_seconds": 3600 } }

### UrlContext

`object` _UrlContext_ — Tool to support URL context.

### McpServer

`object` _McpServer_ — A MCPServer is a server that can be called by the model to perform actions.

It is a server that implements the MCP protocol. Next ID: 6. This data type is
not supported in Vertex AI.
- **name** `string` nullable _Name_ — The name of the MCPServer.
- **streamableHttpTransport** [`StreamableHttpTransport`](#streamablehttptransport) _StreamableHttpTransport_ — A transport that can stream HTTP requests and responses.

Next ID: 6. This data type is not supported in Vertex AI.

### ExternalApi

`object` _ExternalApi_ — Retrieve from data source powered by external API for grounding.

The external API is not owned by Google, but need to follow the pre-defined
API spec. This data type is not supported in Gemini API.
- **apiAuth** [`ApiAuth`](#apiauth) _ApiAuth_ — The generic reusable api auth config.

Deprecated. Please use AuthConfig (google/cloud/aiplatform/master/auth.proto)
instead. This data type is not supported in Gemini API.
- **apiSpec** [`ApiSpec`](#apispec) (`API_SPEC_UNSPECIFIED`, `SIMPLE_SEARCH`, `ELASTIC_SEARCH`) _ApiSpec_ — The API spec that the external API implements.

This enum is not supported in Gemini API.
- **authConfig** [`GoogleGenaiTypesAuthConfig`](#googlegenaitypesauthconfig) _AuthConfig_ — The authentication config to access the API.
- **elasticSearchParams** [`ExternalApiElasticSearchParams`](#externalapielasticsearchparams) _ExternalApiElasticSearchParams_ — The search parameters to use for the ELASTIC_SEARCH spec.

This data type is not supported in Gemini API.
- **endpoint** `string` nullable _Endpoint_ — The endpoint of the external API. The system will call the API at this endpoint to retrieve the data for grounding. Example: https://acme.com:443/search
- **simpleSearchParams** [`ExternalApiSimpleSearchParams`](#externalapisimplesearchparams) _ExternalApiSimpleSearchParams_ — The search parameters to use for SIMPLE_SEARCH spec.

This data type is not supported in Gemini API.

### VertexAISearch

`object` _VertexAISearch_ — Retrieve from Vertex AI Search datastore or engine for grounding.

datastore and engine are mutually exclusive. See
https://cloud.google.com/products/agent-builder. This data type is not
supported in Gemini API.
- **dataStoreSpecs** [`VertexAISearchDataStoreSpec[]`](#vertexaisearchdatastorespec) nullable _Datastorespecs_ — Specifications that define the specific DataStores to be searched, along with configurations for those data stores. This is only considered for Engines with multiple data stores. It should only be set if engine is used.
- **datastore** `string` nullable _Datastore_ — Optional. Fully-qualified Vertex AI Search data store resource ID. Format: `projects/{project}/locations/{location}/collections/{collection}/dataStores/{dataStore}`
- **engine** `string` nullable _Engine_ — Optional. Fully-qualified Vertex AI Search engine resource ID. Format: `projects/{project}/locations/{location}/collections/{collection}/engines/{engine}`
- **filter** `string` nullable _Filter_ — Optional. Filter strings to be passed to the search API.
- **maxResults** `integer` nullable _Maxresults_ — Optional. Number of search results to return per query. The default value is 10. The maximumm allowed value is 10.

### VertexRagStore

`object` _VertexRagStore_ — Retrieve from Vertex RAG Store for grounding.

This data type is not supported in Gemini API.
- **ragCorpora** `string[]` nullable _Ragcorpora_ — Optional. Deprecated. Please use rag_resources instead.
- **ragResources** [`VertexRagStoreRagResource[]`](#vertexragstoreragresource) nullable _Ragresources_ — Optional. The representation of the rag source. It can be used to specify corpus only or ragfiles. Currently only support one corpus or multiple files from one corpus. In the future we may open up multiple corpora support.
- **ragRetrievalConfig** [`RagRetrievalConfig`](#ragretrievalconfig) _RagRetrievalConfig_ — Specifies the context retrieval config.

This data type is not supported in Gemini API.
- **similarityTopK** `integer` nullable _Similaritytopk_ — Optional. Number of top k results to return from the selected corpora.
- **storeContext** `boolean` nullable _Storecontext_ — Optional. Currently only supported for Gemini Multimodal Live API. In Gemini Multimodal Live API, if `store_context` bool is specified, Gemini will leverage it to automatically memorize the interactions between the client and Gemini, and retrieve context when needed to augment the response generation for users' ongoing and future interactions.
- **vectorDistanceThreshold** `number` nullable _Vectordistancethreshold_ — Optional. Only return results with vector distance smaller than the threshold.

### Environment

`string` (`ENVIRONMENT_UNSPECIFIED`, `ENVIRONMENT_BROWSER`, `ENVIRONMENT_MOBILE`, `ENVIRONMENT_DESKTOP`) _Environment_ — The environment being operated.

### SafetyPolicy

`string` (`SAFETY_POLICY_UNSPECIFIED`, `FINANCIAL_TRANSACTIONS`, `SENSITIVE_DATA_MODIFICATION`, `COMMUNICATION_TOOL`, `ACCOUNT_CREATION`, `DATA_MODIFICATION`, `USER_CONSENT_MANAGEMENT`, `LEGAL_TERMS_AND_AGREEMENTS`) _SafetyPolicy_ — SafetyPolicy

### SearchTypes

`object` _SearchTypes_ — Different types of search that can be enabled on the GoogleSearch tool.
- **webSearch** [`WebSearch`](#websearch) _WebSearch_ — Standard web search for grounding and related configurations.

Only text results are returned.
- **imageSearch** [`ImageSearch`](#imagesearch) _ImageSearch_ — Image search for grounding and related configurations.

### PhishBlockThreshold

`string` (`PHISH_BLOCK_THRESHOLD_UNSPECIFIED`, `BLOCK_LOW_AND_ABOVE`, `BLOCK_MEDIUM_AND_ABOVE`, `BLOCK_HIGH_AND_ABOVE`, `BLOCK_HIGHER_AND_ABOVE`, `BLOCK_VERY_HIGH_AND_ABOVE`, `BLOCK_ONLY_EXTREMELY_HIGH`) _PhishBlockThreshold_ — Sites with confidence level chosen & above this value will be blocked from the search results.

This enum is not supported in Gemini API.

### Interval

`object` _Interval_ — Represents a time interval, encoded as a Timestamp start (inclusive) and a Timestamp end (exclusive).

The start must be less than or equal to the end. When the start equals the
end, the interval is empty (matches no time). When both start and end are
unspecified, the interval matches any time.
- **endTime** `string` date-time nullable _Endtime_ — Optional. Exclusive end of the interval. If specified, a Timestamp matching this interval will have to be before the end.
- **startTime** `string` date-time nullable _Starttime_ — Optional. Inclusive start of the interval. If specified, a Timestamp matching this interval will have to be the same or after the start.

### GoogleGenaiTypesAuthConfig

`object` _AuthConfig_ — The authentication config to access the API.
- **apiKey** `string` nullable _Apikey_ — The authentication config to access the API. Only API key is supported. This field is not supported in Gemini API.
- **apiKeyConfig** [`ApiKeyConfig`](#apikeyconfig) _ApiKeyConfig_ — Config for authentication with API key.

This data type is not supported in Gemini API.
- **authType** [`AuthType`](#authtype) (`AUTH_TYPE_UNSPECIFIED`, `NO_AUTH`, `API_KEY_AUTH`, `HTTP_BASIC_AUTH`, `GOOGLE_SERVICE_ACCOUNT_AUTH`, `OAUTH`, `OIDC_AUTH`) _AuthType_ — Type of auth scheme. This enum is not supported in Gemini API.
- **googleServiceAccountConfig** [`AuthConfigGoogleServiceAccountConfig`](#authconfiggoogleserviceaccountconfig) _AuthConfigGoogleServiceAccountConfig_ — Config for Google Service Account Authentication.

This data type is not supported in Gemini API.
- **httpBasicAuthConfig** [`AuthConfigHttpBasicAuthConfig`](#authconfighttpbasicauthconfig) _AuthConfigHttpBasicAuthConfig_ — Config for HTTP Basic Authentication.

This data type is not supported in Gemini API.
- **oauthConfig** [`AuthConfigOauthConfig`](#authconfigoauthconfig) _AuthConfigOauthConfig_ — Config for user oauth. This data type is not supported in Gemini API.
- **oidcConfig** [`AuthConfigOidcConfig`](#authconfigoidcconfig) _AuthConfigOidcConfig_ — Config for user OIDC auth.

This data type is not supported in Gemini API.

### Schema

`object` _Schema_ — Schema is used to define the format of input/output data.

Represents a select subset of an [OpenAPI 3.0 schema
object](https://spec.openapis.org/oas/v3.0.3#schema-object). More fields may
be added in the future as needed.
- **additionalProperties** `unknown` nullable _Additionalproperties_ — Optional. Can either be a boolean or an object; controls the presence of additional properties.
- **defs** `object` nullable _Defs_ — Optional. A map of definitions for use by `ref` Only allowed at the root of the schema.
  - **«additional properties»** [`Schema`](#schema) _Schema_ — Schema is used to define the format of input/output data.

Represents a select subset of an [OpenAPI 3.0 schema
object](https://spec.openapis.org/oas/v3.0.3#schema-object). More fields may
be added in the future as needed.
- **ref** `string` nullable _Ref_ — Optional. Allows indirect references between schema nodes. The value should be a valid reference to a child of the root `defs`. For example, the following schema defines a reference to a schema node named "Pet": type: object properties: pet: ref: #/defs/Pet defs: Pet: type: object properties: name: type: string The value of the "pet" property is a reference to the schema node named "Pet". See details in https://json-schema.org/understanding-json-schema/structuring
- **anyOf** [`Schema[]`](#schema) nullable _Anyof_ — Optional. The instance must be valid against any (one or more) of the subschemas listed in `any_of`.
- **default** `unknown` nullable _Default_ — Optional. Default value to use if the field is not specified.
- **description** `string` nullable _Description_ — Optional. Describes the data. The model uses this field to understand the purpose of the schema and how to use it. It is a best practice to provide a clear and descriptive explanation for the schema and its properties here, rather than in the prompt.
- **enum** `string[]` nullable _Enum_ — Optional. Possible values of the field. This field can be used to restrict a value to a fixed set of values. To mark a field as an enum, set `format` to `enum` and provide the list of possible values in `enum`. For example: 1. To define directions: `{type:STRING, format:enum, enum:["EAST", "NORTH", "SOUTH", "WEST"]}` 2. To define apartment numbers: `{type:INTEGER, format:enum, enum:["101", "201", "301"]}`
- **example** `unknown` nullable _Example_ — Optional. Example of an instance of this schema.
- **format** `string` nullable _Format_ — Optional. The format of the data. For `NUMBER` type, format can be `float` or `double`. For `INTEGER` type, format can be `int32` or `int64`. For `STRING` type, format can be `email`, `byte`, `date`, `date-time`, `password`, and other formats to further refine the data type.
- **items** [`Schema`](#schema) _Schema_ — Schema is used to define the format of input/output data.

Represents a select subset of an [OpenAPI 3.0 schema
object](https://spec.openapis.org/oas/v3.0.3#schema-object). More fields may
be added in the future as needed.
- **maxItems** `integer` nullable _Maxitems_ — Optional. If type is `ARRAY`, `max_items` specifies the maximum number of items in an array.
- **maxLength** `integer` nullable _Maxlength_ — Optional. If type is `STRING`, `max_length` specifies the maximum length of the string.
- **maxProperties** `integer` nullable _Maxproperties_ — Optional. If type is `OBJECT`, `max_properties` specifies the maximum number of properties that can be provided.
- **maximum** `number` nullable _Maximum_ — Optional. If type is `INTEGER` or `NUMBER`, `maximum` specifies the maximum allowed value.
- **minItems** `integer` nullable _Minitems_ — Optional. If type is `ARRAY`, `min_items` specifies the minimum number of items in an array.
- **minLength** `integer` nullable _Minlength_ — Optional. If type is `STRING`, `min_length` specifies the minimum length of the string.
- **minProperties** `integer` nullable _Minproperties_ — Optional. If type is `OBJECT`, `min_properties` specifies the minimum number of properties that can be provided.
- **minimum** `number` nullable _Minimum_ — Optional. If type is `INTEGER` or `NUMBER`, `minimum` specifies the minimum allowed value.
- **nullable** `boolean` nullable _Nullable_ — Optional. Indicates if the value of this field can be null.
- **pattern** `string` nullable _Pattern_ — Optional. If type is `STRING`, `pattern` specifies a regular expression that the string must match.
- **properties** `object` nullable _Properties_ — Optional. If type is `OBJECT`, `properties` is a map of property names to schema definitions for each property of the object.
  - **«additional properties»** [`Schema`](#schema) _Schema_ — Schema is used to define the format of input/output data.

Represents a select subset of an [OpenAPI 3.0 schema
object](https://spec.openapis.org/oas/v3.0.3#schema-object). More fields may
be added in the future as needed.
- **propertyOrdering** `string[]` nullable _Propertyordering_ — Optional. Order of properties displayed or used where order matters. This is not a standard field in OpenAPI specification, but can be used to control the order of properties.
- **required** `string[]` nullable _Required_ — Optional. If type is `OBJECT`, `required` lists the names of properties that must be present.
- **title** `string` nullable _Title_ — Optional. Title for the schema.
- **type** [`Type`](#type) (`TYPE_UNSPECIFIED`, `STRING`, `NUMBER`, `INTEGER`, `BOOLEAN`, `ARRAY`, `OBJECT`, `NULL`) _Type_ — Data type of the schema field.

### Behavior

`string` (`UNSPECIFIED`, `BLOCKING`, `NON_BLOCKING`) _Behavior_ — Specifies the function Behavior.

Currently only non-blocking functions are supported. If not specified, the
system keeps the current function call behavior. This field is currently only
supported by the BidiGenerateContent method.

### DynamicRetrievalConfig

`object` _DynamicRetrievalConfig_ — Describes the options to customize dynamic retrieval.
- **dynamicThreshold** `number` nullable _Dynamicthreshold_ — Optional. The threshold to be used in dynamic retrieval. If not set, a system default value is used.
- **mode** [`DynamicRetrievalConfigMode`](#dynamicretrievalconfigmode) (`MODE_UNSPECIFIED`, `MODE_DYNAMIC`) _DynamicRetrievalConfigMode_ — The mode of the predictor to be used in dynamic retrieval.

### StreamableHttpTransport

`object` _StreamableHttpTransport_ — A transport that can stream HTTP requests and responses.

Next ID: 6. This data type is not supported in Vertex AI.
- **headers** `object` nullable _Headers_ — Optional: Fields for authentication headers, timeouts, etc., if needed.
  - **«additional properties»** `string`
- **sseReadTimeout** `string` nullable _Ssereadtimeout_ — Timeout for SSE read operations.
- **terminateOnClose** `boolean` nullable _Terminateonclose_ — Whether to close the client session when the transport closes.
- **timeout** `string` nullable _Timeout_ — HTTP timeout for regular operations.
- **url** `string` nullable _Url_ — The full URL for the MCPServer endpoint. Example: "https://api.example.com/mcp".

### ApiAuth

`object` _ApiAuth_ — The generic reusable api auth config.

Deprecated. Please use AuthConfig (google/cloud/aiplatform/master/auth.proto)
instead. This data type is not supported in Gemini API.
- **apiKeyConfig** [`ApiAuthApiKeyConfig`](#apiauthapikeyconfig) _ApiAuthApiKeyConfig_ — The API secret. This data type is not supported in Gemini API.

### ApiSpec

`string` (`API_SPEC_UNSPECIFIED`, `SIMPLE_SEARCH`, `ELASTIC_SEARCH`) _ApiSpec_ — The API spec that the external API implements.

This enum is not supported in Gemini API.

### ExternalApiElasticSearchParams

`object` _ExternalApiElasticSearchParams_ — The search parameters to use for the ELASTIC_SEARCH spec.

This data type is not supported in Gemini API.
- **index** `string` nullable _Index_ — The ElasticSearch index to use.
- **numHits** `integer` nullable _Numhits_ — Optional. Number of hits (chunks) to request. When specified, it is passed to Elasticsearch as the `num_hits` param.
- **searchTemplate** `string` nullable _Searchtemplate_ — The ElasticSearch search template to use.

### ExternalApiSimpleSearchParams

`object` _ExternalApiSimpleSearchParams_ — The search parameters to use for SIMPLE_SEARCH spec.

This data type is not supported in Gemini API.

### VertexAISearchDataStoreSpec

`object` _VertexAISearchDataStoreSpec_ — Define data stores within engine to filter on in a search call and configurations for those data stores.

For more information, see
https://cloud.google.com/generative-ai-app-builder/docs/reference/rpc/google.cloud.discoveryengine.v1#datastorespec.
This data type is not supported in Gemini API.
- **dataStore** `string` nullable _Datastore_ — Full resource name of DataStore, such as Format: `projects/{project}/locations/{location}/collections/{collection}/dataStores/{dataStore}`
- **filter** `string` nullable _Filter_ — Optional. Filter specification to filter documents in the data store specified by data_store field. For more information on filtering, see [Filtering](https://cloud.google.com/generative-ai-app-builder/docs/filter-search-metadata)

### VertexRagStoreRagResource

`object` _VertexRagStoreRagResource_ — The definition of the Rag resource.

This data type is not supported in Gemini API.
- **ragCorpus** `string` nullable _Ragcorpus_ — Optional. RagCorpora resource name. Format: `projects/{project}/locations/{location}/ragCorpora/{rag_corpus}`
- **ragFileIds** `string[]` nullable _Ragfileids_ — Optional. rag_file_id. The files should be in the same rag_corpus set in rag_corpus field.

### RagRetrievalConfig

`object` _RagRetrievalConfig_ — Specifies the context retrieval config.

This data type is not supported in Gemini API.
- **filter** [`RagRetrievalConfigFilter`](#ragretrievalconfigfilter) _RagRetrievalConfigFilter_ — Config for filters. This data type is not supported in Gemini API.
- **hybridSearch** [`RagRetrievalConfigHybridSearch`](#ragretrievalconfighybridsearch) _RagRetrievalConfigHybridSearch_ — Config for Hybrid Search. This data type is not supported in Gemini API.
- **ranking** [`RagRetrievalConfigRanking`](#ragretrievalconfigranking) _RagRetrievalConfigRanking_ — Config for ranking and reranking.

This data type is not supported in Gemini API.
- **topK** `integer` nullable _Topk_ — Optional. The number of contexts to retrieve.

### WebSearch

`object` _WebSearch_ — Standard web search for grounding and related configurations.

Only text results are returned.

### ImageSearch

`object` _ImageSearch_ — Image search for grounding and related configurations.

### ApiKeyConfig

`object` _ApiKeyConfig_ — Config for authentication with API key.

This data type is not supported in Gemini API.
- **apiKeySecret** `string` nullable _Apikeysecret_ — Optional. The name of the SecretManager secret version resource storing the API key. Format: `projects/{project}/secrets/{secrete}/versions/{version}` - If both `api_key_secret` and `api_key_string` are specified, this field takes precedence over `api_key_string`. - If specified, the `secretmanager.versions.access` permission should be granted to Vertex AI Extension Service Agent (https://cloud.google.com/vertex-ai/docs/general/access-control#service-agents) on the specified resource.
- **apiKeyString** `string` nullable _Apikeystring_ — Optional. The API key to be used in the request directly.
- **httpElementLocation** [`HttpElementLocation`](#httpelementlocation) (`HTTP_IN_UNSPECIFIED`, `HTTP_IN_QUERY`, `HTTP_IN_HEADER`, `HTTP_IN_PATH`, `HTTP_IN_BODY`, `HTTP_IN_COOKIE`) _HttpElementLocation_ — The location of the API key. This enum is not supported in Gemini API.
- **name** `string` nullable _Name_ — Optional. The parameter name of the API key. E.g. If the API request is "https://example.com/act?api_key=", "api_key" would be the parameter name.

### AuthType

`string` (`AUTH_TYPE_UNSPECIFIED`, `NO_AUTH`, `API_KEY_AUTH`, `HTTP_BASIC_AUTH`, `GOOGLE_SERVICE_ACCOUNT_AUTH`, `OAUTH`, `OIDC_AUTH`) _AuthType_ — Type of auth scheme. This enum is not supported in Gemini API.

### AuthConfigGoogleServiceAccountConfig

`object` _AuthConfigGoogleServiceAccountConfig_ — Config for Google Service Account Authentication.

This data type is not supported in Gemini API.
- **serviceAccount** `string` nullable _Serviceaccount_ — Optional. The service account that the extension execution service runs as. - If the service account is specified, the `iam.serviceAccounts.getAccessToken` permission should be granted to Vertex AI Extension Service Agent (https://cloud.google.com/vertex-ai/docs/general/access-control#service-agents) on the specified service account. - If not specified, the Vertex AI Extension Service Agent will be used to execute the Extension.

### AuthConfigHttpBasicAuthConfig

`object` _AuthConfigHttpBasicAuthConfig_ — Config for HTTP Basic Authentication.

This data type is not supported in Gemini API.
- **credentialSecret** `string` nullable _Credentialsecret_ — Required. The name of the SecretManager secret version resource storing the base64 encoded credentials. Format: `projects/{project}/secrets/{secrete}/versions/{version}` - If specified, the `secretmanager.versions.access` permission should be granted to Vertex AI Extension Service Agent (https://cloud.google.com/vertex-ai/docs/general/access-control#service-agents) on the specified resource.

### AuthConfigOauthConfig

`object` _AuthConfigOauthConfig_ — Config for user oauth. This data type is not supported in Gemini API.
- **accessToken** `string` nullable _Accesstoken_ — Access token for extension endpoint. Only used to propagate token from [[ExecuteExtensionRequest.runtime_auth_config]] at request time.
- **serviceAccount** `string` nullable _Serviceaccount_ — The service account used to generate access tokens for executing the Extension. - If the service account is specified, the `iam.serviceAccounts.getAccessToken` permission should be granted to Vertex AI Extension Service Agent (https://cloud.google.com/vertex-ai/docs/general/access-control#service-agents) on the provided service account.

### AuthConfigOidcConfig

`object` _AuthConfigOidcConfig_ — Config for user OIDC auth.

This data type is not supported in Gemini API.
- **idToken** `string` nullable _Idtoken_ — OpenID Connect formatted ID token for extension endpoint. Only used to propagate token from [[ExecuteExtensionRequest.runtime_auth_config]] at request time.
- **serviceAccount** `string` nullable _Serviceaccount_ — The service account used to generate an OpenID Connect (OIDC)-compatible JWT token signed by the Google OIDC Provider (accounts.google.com) for extension endpoint (https://cloud.google.com/iam/docs/create-short-lived-credentials-direct#sa-credentials-oidc). - The audience for the token will be set to the URL in the server url defined in the OpenApi spec. - If the service account is provided, the service account should grant `iam.serviceAccounts.getOpenIdToken` permission to Vertex AI Extension Service Agent (https://cloud.google.com/vertex-ai/docs/general/access-control#service-agents).

### Type

`string` (`TYPE_UNSPECIFIED`, `STRING`, `NUMBER`, `INTEGER`, `BOOLEAN`, `ARRAY`, `OBJECT`, `NULL`) _Type_ — Data type of the schema field.

### DynamicRetrievalConfigMode

`string` (`MODE_UNSPECIFIED`, `MODE_DYNAMIC`) _DynamicRetrievalConfigMode_ — The mode of the predictor to be used in dynamic retrieval.

### ApiAuthApiKeyConfig

`object` _ApiAuthApiKeyConfig_ — The API secret. This data type is not supported in Gemini API.
- **apiKeySecretVersion** `string` nullable _Apikeysecretversion_ — Required. The SecretManager secret version resource name storing API key. e.g. projects/{project}/secrets/{secret}/versions/{version}
- **apiKeyString** `string` nullable _Apikeystring_ — The API key string. Either this or `api_key_secret_version` must be set.

### RagRetrievalConfigFilter

`object` _RagRetrievalConfigFilter_ — Config for filters. This data type is not supported in Gemini API.
- **metadataFilter** `string` nullable _Metadatafilter_ — Optional. String for metadata filtering.
- **vectorDistanceThreshold** `number` nullable _Vectordistancethreshold_ — Optional. Only returns contexts with vector distance smaller than the threshold.
- **vectorSimilarityThreshold** `number` nullable _Vectorsimilaritythreshold_ — Optional. Only returns contexts with vector similarity larger than the threshold.

### RagRetrievalConfigHybridSearch

`object` _RagRetrievalConfigHybridSearch_ — Config for Hybrid Search. This data type is not supported in Gemini API.
- **alpha** `number` nullable _Alpha_ — Optional. Alpha value controls the weight between dense and sparse vector search results. The range is [0, 1], while 0 means sparse vector search only and 1 means dense vector search only. The default value is 0.5 which balances sparse and dense vector search equally.

### RagRetrievalConfigRanking

`object` _RagRetrievalConfigRanking_ — Config for ranking and reranking.

This data type is not supported in Gemini API.
- **llmRanker** [`RagRetrievalConfigRankingLlmRanker`](#ragretrievalconfigrankingllmranker) _RagRetrievalConfigRankingLlmRanker_ — Config for LlmRanker. This data type is not supported in Gemini API.
- **rankService** [`RagRetrievalConfigRankingRankService`](#ragretrievalconfigrankingrankservice) _RagRetrievalConfigRankingRankService_ — Config for Rank Service. This data type is not supported in Gemini API.

### HttpElementLocation

`string` (`HTTP_IN_UNSPECIFIED`, `HTTP_IN_QUERY`, `HTTP_IN_HEADER`, `HTTP_IN_PATH`, `HTTP_IN_BODY`, `HTTP_IN_COOKIE`) _HttpElementLocation_ — The location of the API key. This enum is not supported in Gemini API.

### RagRetrievalConfigRankingLlmRanker

`object` _RagRetrievalConfigRankingLlmRanker_ — Config for LlmRanker. This data type is not supported in Gemini API.
- **modelName** `string` nullable _Modelname_ — Optional. The model name used for ranking. See [Supported models](https://cloud.google.com/vertex-ai/generative-ai/docs/model-reference/inference#supported-models).

### RagRetrievalConfigRankingRankService

`object` _RagRetrievalConfigRankingRankService_ — Config for Rank Service. This data type is not supported in Gemini API.
- **modelName** `string` nullable _Modelname_ — Optional. The model name of the rank service. Format: `semantic-ranker-512@latest`